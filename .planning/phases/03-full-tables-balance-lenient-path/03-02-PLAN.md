---
phase: 03
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/contaplus_reader/models.py
  - src/contaplus_reader/_balance.py
  - src/contaplus_reader/_reader.py
autonomous: true
requirements:
  - BAL-02
  - API-03
  - TABL-03

must_haves:
  truths:
    - "ProblemEntry, ProblemsReport, BalanceRow, BalanceTable dataclasses exist in models.py"
    - "ContaPlusData has new attributes: balan, venci, prede, amoinv, nivel, balance_cuenta, balance_subcuenta, problems"
    - "compute_balance() in _balance.py uses Decimal(str(float)) accumulation and returns correct saldo_deudor/saldo_acreedor"
    - "_read_dbf_path() accepts lenient=True and skips per-row errors into the problems list; file-level errors (row_index==-1) still propagate"
    - "_build_journal_row() helper extracted from the D-C5 loop"
    - "test_balance.py and relevant test_lenient.py tests pass (GREEN)"
  artifacts:
    - path: "src/contaplus_reader/models.py"
      provides: "ProblemEntry, ProblemsReport, BalanceRow, BalanceTable types + ContaPlusData new attributes"
      contains: "class ProblemEntry"
    - path: "src/contaplus_reader/_balance.py"
      provides: "compute_balance() — Decimal trial balance computation"
      exports: ["compute_balance"]
    - path: "src/contaplus_reader/_reader.py"
      provides: "Lenient journal loop (_build_journal_row helper + lenient/problems params)"
      contains: "def _build_journal_row"
  key_links:
    - from: "src/contaplus_reader/_reader.py"
      to: "src/contaplus_reader/models.py"
      via: "import ProblemEntry"
      pattern: "from contaplus_reader.models import.*ProblemEntry"
    - from: "src/contaplus_reader/_balance.py"
      to: "src/contaplus_reader/models.py"
      via: "import BalanceRow, BalanceTable, ContaPlusJournal"
      pattern: "from contaplus_reader.models import BalanceRow"
    - from: "src/contaplus_reader/models.py"
      to: "decimal"
      via: "from decimal import Decimal"
      pattern: "from decimal import Decimal"
---

<objective>
Implement the core model types (ProblemEntry, ProblemsReport, BalanceRow, BalanceTable), extend ContaPlusData with all Phase 3 attributes, create the _balance.py computation module, and extend _reader.py with the lenient journal loop.

Purpose: This is the data-contract and core-logic layer. Plans 03 and 04 depend on these types existing. The BAL-02 Decimal computation is isolated here with no I/O. The lenient journal loop refactoring is self-contained in _reader.py.
Output: models.py with Phase 3 types; new _balance.py module; extended _reader.py with _build_journal_row helper and lenient/problems parameters.
</objective>

<execution_context>
@C:/dev/a3api/.claude/gsd/plugins/gsd/get-shit-done/workflows/execute-plan.md
@C:/dev/a3api/.claude/gsd/plugins/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-full-tables-balance-lenient-path/03-CONTEXT.md
@.planning/phases/03-full-tables-balance-lenient-path/03-RESEARCH.md
@.planning/phases/03-full-tables-balance-lenient-path/03-PATTERNS.md

<interfaces>
<!-- Existing models.py patterns the executor must extend (do not break) -->

From src/contaplus_reader/models.py (existing structure):
```python
@dataclass
class ContaPlusData:
    """NOT frozen — grows new attributes in Phases 2-3."""
    journal: ContaPlusJournal | None = None
    subcta: SubctaTable | None = None
    empresa: GenericTable | None = None
    grupos: GenericTable | None = None
    usuarios: GenericTable | None = None
```

Existing frozen dataclass pattern:
```python
@dataclass(frozen=True)
class JournalRow:
    fecha: datetime.date
    cuenta: str
    subcuenta: str
    debe: float
    haber: float
    concepto: str | None
    subcuenta_nombre: str | None = None
```

From src/contaplus_reader/_reader.py (existing D-C5 loop structure):
- `_read_dbf_path(path, *, source_name, subcta_lookup)` — current signature; returns ContaPlusJournal
- The D-C5 validation loop is the `for idx, record in enumerate(table):` block
- ContaPlusReadError is raised on: null FECHA (row_index=idx), both-non-zero amounts (row_index=idx), malformed subcuenta (row_index=idx), file-level open errors (row_index=-1)

From src/contaplus_reader/_subcta.py (module header pattern for _balance.py):
```python
"""Module docstring."""
from __future__ import annotations
import logging
from contaplus_reader.models import ...
logger = logging.getLogger("contaplus_reader._subcta")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend models.py — Phase 3 types and ContaPlusData attributes</name>
  <files>src/contaplus_reader/models.py</files>
  <read_first>
    src/contaplus_reader/models.py
    .planning/phases/03-full-tables-balance-lenient-path/03-RESEARCH.md
    .planning/phases/03-full-tables-balance-lenient-path/03-PATTERNS.md
  </read_first>
  <behavior>
    - ProblemEntry frozen dataclass: fields table: str, row_index: int, column: str, reason: str, value: str
    - ProblemsReport frozen dataclass: field entries: tuple[ProblemEntry, ...]
    - BalanceRow frozen dataclass: fields code: str, suma_debe: Decimal, suma_haber: Decimal, saldo_deudor: Decimal, saldo_acreedor: Decimal, saldo: Decimal, descripcion: str | None = None
    - BalanceTable frozen dataclass: fields rows: tuple[BalanceRow, ...], level: str  (level is "cuenta" or "subcuenta")
    - ContaPlusData gains nine new optional attributes with None defaults: balan: GenericTable | None = None, venci: GenericTable | None = None, prede: GenericTable | None = None, amoinv: GenericTable | None = None, nivel: GenericTable | None = None, balance_cuenta: BalanceTable | None = None, balance_subcuenta: BalanceTable | None = None, problems: ProblemsReport | None = None
    - models.py imports gain `from decimal import Decimal` (add to existing imports block)
    - All new types are exported (importable from contaplus_reader.models)
    - Existing types (ContaPlusJournal, JournalRow, SubctaTable, GenericTable, ContaPlusReadError) are unchanged
  </behavior>
  <action>Read models.py in full first. Add `from decimal import Decimal` to the imports block. Append four new frozen dataclasses after the existing types: ProblemEntry (table, row_index, column, reason, value), ProblemsReport (entries: tuple[ProblemEntry, ...]), BalanceRow (code, suma_debe, suma_haber, saldo_deudor, saldo_acreedor, saldo, descripcion=None — all Decimal fields), BalanceTable (rows: tuple[BalanceRow, ...], level: str). Extend ContaPlusData by adding eight new fields with None defaults after the existing usuarios field: balan, venci, prede, amoinv, nivel (all GenericTable | None = None), balance_cuenta, balance_subcuenta (BalanceTable | None = None), problems (ProblemsReport | None = None). Do not modify any existing field or type. Follow the frozen=True pattern from JournalRow for all new types. ContaPlusData remains NOT frozen (mutable container per Phase-1 D-03).</action>
  <verify>
    <automated>uv run pytest tests/test_balance.py tests/test_lenient.py -x -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>models.py contains ProblemEntry, ProblemsReport, BalanceRow, BalanceTable classes; ContaPlusData has 8 new None-defaulted attributes; `from contaplus_reader.models import ProblemEntry, BalanceRow, BalanceTable` succeeds without error; existing Phase 1/2 tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create _balance.py — compute_balance with Decimal accumulation</name>
  <files>src/contaplus_reader/_balance.py</files>
  <read_first>
    src/contaplus_reader/_subcta.py
    src/contaplus_reader/models.py
    .planning/phases/03-full-tables-balance-lenient-path/03-RESEARCH.md
    .planning/phases/03-full-tables-balance-lenient-path/03-PATTERNS.md
  </read_first>
  <behavior>
    - compute_balance(journal: ContaPlusJournal, *, by_subcuenta: bool = False, subcta_lookup: dict[str, str | None] | None = None) -> BalanceTable
    - Groups journal.rows by cuenta (row.cuenta, 4-digit) when by_subcuenta=False; by row.subcuenta (full code) when by_subcuenta=True
    - Accumulates debe and haber using Decimal(str(row.debe)) and Decimal(str(row.haber)) — mandatory; never Decimal(float_value) directly
    - For each account code, computes: saldo_deudor = max(suma_debe - suma_haber, Decimal("0")); saldo_acreedor = max(suma_haber - suma_debe, Decimal("0")); saldo = suma_debe - suma_haber (positive = deudor, negative = acreedor per D-08)
    - Returns BalanceTable with rows sorted ascending by code (alphabetic string sort); level="cuenta" or level="subcuenta"
    - When by_subcuenta=True and subcta_lookup is provided: BalanceRow.descripcion = subcta_lookup.get(key) for each code; when subcta_lookup is None or empty: descripcion=None
    - No ContaPlusReadError wrapping — operates on already-validated ContaPlusJournal.rows (no I/O); any exceptions propagate naturally
    - Module: `"""Trial balance (sumas y saldos) computation — BAL-02."""`; `from __future__ import annotations`; logger = logging.getLogger("contaplus_reader._balance")
  </behavior>
  <action>Create src/contaplus_reader/_balance.py from scratch using _subcta.py as structural template (module docstring, from __future__ import annotations, logging, logger = getLogger). Import from decimal import Decimal; from collections import defaultdict; from contaplus_reader.models import BalanceRow, BalanceTable, ContaPlusJournal. Implement compute_balance() using defaultdict(Decimal) for debe and haber accumulators. Use Decimal(str(row.debe)) and Decimal(str(row.haber)) per RESEARCH.md §Pitfall 2 — never Decimal(float_value). Build BalanceRows with max(sd - sh, Decimal("0")) for saldo_deudor and max(sh - sd, Decimal("0")) for saldo_acreedor. Sort by key using sorted(). Return BalanceTable(rows=tuple(result), level="subcuenta" if by_subcuenta else "cuenta").</action>
  <verify>
    <automated>uv run pytest tests/test_balance.py -x -q</automated>
  </verify>
  <done>tests/test_balance.py passes completely (all 8 tests GREEN); `from contaplus_reader._balance import compute_balance` succeeds; test_decimal_precision confirms Decimal("0.3") result; test_saldo_deudor_acreedor confirms correct split; test_by_subcuenta_descripcion confirms Descripción populated from lookup</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend _reader.py — _build_journal_row helper + lenient/problems parameters</name>
  <files>src/contaplus_reader/_reader.py</files>
  <read_first>
    src/contaplus_reader/_reader.py
    src/contaplus_reader/models.py
    .planning/phases/03-full-tables-balance-lenient-path/03-RESEARCH.md
    .planning/phases/03-full-tables-balance-lenient-path/03-PATTERNS.md
  </read_first>
  <behavior>
    - _build_journal_row(record, idx, debe_col, haber_col, subcta_lookup) -> JournalRow | None: extracts per-row validation logic currently inline in the D-C5 loop; raises ContaPlusReadError with the appropriate row_index=idx and column when validation fails; returns None for both-zero memo rows (D-C3); returns JournalRow on success
    - _read_dbf_path signature gains two new keyword-only parameters with defaults: lenient: bool = False, problems: list[ProblemEntry] | None = None
    - The D-C5 loop calls _build_journal_row(); on ContaPlusReadError in lenient mode: if exc.row_index == -1 (file-level), re-raise regardless of lenient; if exc.row_index >= 0 (per-row), append ProblemEntry(table="DIARIO", row_index=idx, column=exc.column or "", reason=exc.message, value=str(_raw_value(record, exc.column))) to problems if problems is not None, then continue
    - Memo skips (row is None from _build_journal_row) increment skipped_memo and do NOT append a ProblemEntry (D-05)
    - Strict mode behavior (lenient=False): identical to current behavior — any ContaPlusReadError propagates immediately; no behavior change to existing callers
    - Import ProblemEntry from contaplus_reader.models at the top of _reader.py (add to existing import block)
    - A helper _raw_value(record, column: str | None) -> str that returns str(record.get(column, "")) if column else ""
  </behavior>
  <action>Read _reader.py in full first. Add ProblemEntry to the import from contaplus_reader.models. Extract the per-row validation logic from the D-C5 `for idx, record in enumerate(table):` loop into a new private function `_build_journal_row(record, idx, debe_col, haber_col, subcta_lookup)` — this function contains exactly the current validation code that raises ContaPlusReadError or returns JournalRow | None. Also add `_raw_value(record, column)` helper. Extend `_read_dbf_path()` signature with `lenient: bool = False, problems: list[ProblemEntry] | None = None` parameters. In the loop body call `_build_journal_row(...)` and wrap it with the lenient catch: re-raise when `exc.row_index == -1`, collect-and-continue when per-row. The outer try/except block that wraps the entire loop (catching UnicodeDecodeError, struct.error, etc.) remains unchanged — these are file-level errors that always propagate (row_index=-1). All existing callers of _read_dbf_path() continue to work because lenient=False and problems=None are defaults.</action>
  <verify>
    <automated>uv run pytest tests/test_lenient.py::test_lenient_bad_journal_row_skipped tests/test_lenient.py::test_strict_still_raises_on_bad_row tests/test_reader.py -x -q</automated>
  </verify>
  <done>test_lenient_bad_journal_row_skipped passes: 2 rows in journal, 1 problems entry with table="DIARIO" and row_index=1; test_strict_still_raises_on_bad_row passes: ContaPlusReadError raised with row_index=1; all existing test_reader.py tests still pass (strict behavior unchanged)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| journal rows → _build_journal_row | Each row is untrusted DBF data; validation still raises ContaPlusReadError in strict mode |
| lenient mode skip | Per-row errors become ProblemEntry instead of abort; file-level errors still propagate (D-03 / Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | _build_journal_row — null FECHA / bad subcuenta | mitigate | Validation logic unchanged from Phase 1/2; extracted to helper, not relaxed; strict mode still raises ContaPlusReadError |
| T-03-04 | Tampering | lenient mode swallows file-level errors | mitigate | Explicit check: `if exc.row_index == -1: raise` inside lenient catch (RESEARCH.md Pitfall 3); confirmed by test_lenient_corrupt_journal |
| T-03-05 | Tampering | BalanceRow Decimal fields accept arbitrary input | accept | compute_balance operates on already-validated JournalRow.debe/haber floats; Decimal(str(float)) is safe for any finite float |
| T-03-06 | Denial of Service | Very long problem entry reason strings | accept | Reason strings come from ContaPlusReadError.message (program-generated) or field names; not from raw user data |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 3 |
</threat_model>

<verification>
After all three tasks:
- `uv run pytest tests/test_balance.py -v` — all 8 tests GREEN
- `uv run pytest tests/test_lenient.py::test_lenient_bad_journal_row_skipped tests/test_lenient.py::test_strict_still_raises_on_bad_row -v` — GREEN
- `uv run pytest tests/test_reader.py -v` — all existing reader tests still GREEN
- `uv run pytest tests/ -x -q` — Phase 1/2 tests green; Phase 3 remaining tests still RED (Plans 03-04 not yet implemented)
</verification>

<success_criteria>
- models.py contains ProblemEntry, ProblemsReport, BalanceRow, BalanceTable; ContaPlusData has 8 new None-defaulted attributes
- _balance.py exists with compute_balance(); all test_balance.py tests GREEN
- _reader.py has _build_journal_row() private helper + lenient/problems parameters; lenient loop skips per-row errors; file-level errors propagate in all modes
- test_lenient_bad_journal_row_skipped and test_strict_still_raises_on_bad_row GREEN
- Zero regressions in existing Phase 1/2 test suite
</success_criteria>

<output>
Create `.planning/phases/03-full-tables-balance-lenient-path/03-02-SUMMARY.md` when done
</output>
