---
phase: 03-full-tables-balance-lenient-path
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/contaplus_reader/xlsx.py
  - src/contaplus_reader/_reader.py
  - src/contaplus_reader/__init__.py
  - src/contaplus_reader/cli.py
  - tests/test_xlsx.py
  - tests/test_lenient.py
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - "render(ContaPlusData(journal=None)) succeeds and returns valid workbook bytes containing a 'Diario' sheet with headers only (WR-06 fix)"
    - "A lenient read() of a corrupt-DIARIO ZIP followed by render() produces a valid workbook with a Diario sheet (headers only), a Subcuentas sheet, and a Problemas sheet (D-03 end-to-end)"
    - "In lenient mode, a dbfread ValueError raised during per-row iteration skips only that row and records a ProblemEntry, not a file-level abort (WR-05 fix)"
    - "ProblemEntry.table is always an uppercase stem without extension at every call site: 'DIARIO', 'BALAN', 'VENCI', 'PREDE', 'AMOINV', 'NIVEL', 'GRUPOS', 'USUARIOS', 'EMPRESA', and uppercase stem for uncatalogued files"
    - "CLI one-line summary reports the correct sheet count matching the number of sheets render() actually emitted"
  artifacts:
    - path: "src/contaplus_reader/xlsx.py"
      provides: "render() None guard for data.journal — headers-only Diario sheet when journal is None"
      contains: "if data.journal is not None"
    - path: "src/contaplus_reader/_reader.py"
      provides: "per-row ValueError catch in lenient mode — dbfread ValueError skipped as ProblemEntry"
      contains: "except (ContaPlusReadError, ValueError)"
    - path: "src/contaplus_reader/__init__.py"
      provides: "canonical ProblemEntry.table convention — uppercase stem at all call sites"
      contains: "table_name.upper().removesuffix"
    - path: "src/contaplus_reader/cli.py"
      provides: "sheet_count derived from rendered workbook via load_workbook"
      contains: "load_workbook"
    - path: "tests/test_xlsx.py"
      provides: "render(journal=None) unit test and end-to-end lenient read+render test"
      contains: "test_render_journal_none_produces_valid_workbook"
    - path: "tests/test_lenient.py"
      provides: "WR-05 dbfread ValueError test; updated table-casing assertions"
      contains: "test_lenient_dbfread_valueerror_skipped"
  key_links:
    - from: "src/contaplus_reader/xlsx.py render()"
      to: "_render_journal_sheet"
      via: "None guard: if data.journal is not None"
      pattern: "if data.journal is not None"
    - from: "src/contaplus_reader/_reader.py per-row loop"
      to: "ProblemEntry collector"
      via: "except (ContaPlusReadError, ValueError)"
      pattern: "except.*ContaPlusReadError.*ValueError"
    - from: "src/contaplus_reader/__init__.py _read_secondary_table"
      to: "ProblemEntry.table"
      via: "table_name.upper().removesuffix('.DBF').removesuffix('.dbf')"
      pattern: "removesuffix"
    - from: "src/contaplus_reader/cli.py"
      to: "load_workbook(io.BytesIO(xlsx_bytes)).sheetnames"
      via: "sheet count from rendered workbook"
      pattern: "load_workbook"
---

<objective>
Close four verified gaps from Phase 3 verification: fix the render() crash when
journal=None (BLOCKER — WR-06), fix per-row ValueError not caught by the lenient
handler (WR-05), canonicalize ProblemEntry.table casing (WARNING), and fix the
CLI sheet count undercount (WARNING).

Purpose: The D-03 end-to-end contract ("lenient conversion of a corrupt-DIARIO ZIP
produces a workbook") is broken at render time. Fixing WR-06 restores API-03 fully.
The warning fixes prevent future consumer confusion and test fragility.

Output: render() never crashes on journal=None; lenient mode skips dbfread
ValueErrors at the row level; ProblemEntry.table uses a single canonical uppercase
stem convention; CLI sheet count is always accurate.
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/marc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-full-tables-balance-lenient-path/03-CONTEXT.md
@.planning/phases/03-full-tables-balance-lenient-path/03-VERIFICATION.md
@.planning/phases/03-full-tables-balance-lenient-path/03-04-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from current source. -->

From src/contaplus_reader/xlsx.py render() (line 84-126):
  - wb.active is the first sheet; must always be titled "Diario"
  - _render_journal_sheet(ws, journal) writes headers + data rows; ws.title = "Diario" is set INSIDE it
  - When journal is None, call _render_journal_sheet with None is currently uncaught (WR-06)
  - Fix pattern: guard before _render_journal_sheet, set title and headers manually in the else branch

From src/contaplus_reader/_reader.py (lines 290-320):
  - Inner per-row try/except at lines 291-313 only catches ContaPlusReadError
  - Outer except at lines 343-358 catches (struct.error, ValueError, OSError) as file-level
  - dbfread can raise ValueError during record deserialization (field type mismatch)
  - Fix: change inner except to (ContaPlusReadError, ValueError); when exc is ValueError,
    fabricate a ProblemEntry directly (no .row_index / .column attrs available)

From src/contaplus_reader/__init__.py:
  - _read_secondary_table (lines 62-99): table_name argument is e.g. "grupos.dbf", "BALAN.DBF"
    -> ProblemEntry(table=table_name.lower(), ...) at line 96 — needs canonical fix
  - D-03 DIARIO block (lines 179-189): ProblemEntry(table="DIARIO", ...) — already correct (uppercase stem)
  - D-13 uncatalogued block (lines 241-255): ProblemEntry(table=candidate.name.upper(), ...)
    — "EXTRA.DBF" should become "EXTRA" (stem only)
  - Canonical helper: path.stem.upper() or table_name.upper().removesuffix(".DBF") where
    table_name already ends in .dbf/.DBF

From src/contaplus_reader/cli.py (lines 123-137):
  - xlsx_bytes is already produced before the sheet_count line
  - Fix: import openpyxl.load_workbook at top of cli.py (or inside the function),
    parse xlsx_bytes to count sheetnames: len(load_workbook(io.BytesIO(xlsx_bytes)).sheetnames)
  - io is in stdlib; openpyxl is already a runtime dep; no new imports needed at module level
    (import inside function to keep startup fast, consistent with existing pattern of
    importing read and render inside main())

From tests/test_lenient.py:
  - Line 113: e.table.lower() == "venci.dbf" — after casing fix, update to e.table == "VENCI"
  - Line 152: e.table == "DIARIO" — already correct, no change needed
  - Line 176: e.table.upper() == "EXTRA.DBF" — after casing fix, update to e.table == "EXTRA"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix render() crash on journal=None and add end-to-end tests (WR-06)</name>
  <files>src/contaplus_reader/xlsx.py, tests/test_xlsx.py</files>

  <read_first>
    src/contaplus_reader/xlsx.py
    tests/test_xlsx.py
    .planning/phases/03-full-tables-balance-lenient-path/03-VERIFICATION.md (WR-06 section)
  </read_first>

  <behavior>
    xlsx.py render() changes:
    - Line 87 currently: _render_journal_sheet(wb.active, data.journal)  # type: ignore[arg-type]
    - Replace with: if data.journal is not None: _render_journal_sheet(wb.active, data.journal)
      else: ws = wb.active; ws.title = "Diario"; _write_diario_headers_only(ws)
    - Extract the header-writing logic into a private helper _write_diario_headers_only(ws)
      that writes HEADERS row 1 with HEADER_FILL + HEADER_FONT, sets freeze_panes = "A2",
      and calls _autosize_columns(ws) — identical to what _render_journal_sheet does for row 1,
      but with no data rows. This avoids duplicating the styling logic inline.
    - Remove the # type: ignore[arg-type] suppression comment — it is no longer needed once
      the None guard makes the call type-sound.
    - _render_journal_sheet signature and body are unchanged.

    test_xlsx.py new tests (add after existing Phase 3 tests):

    test_render_journal_none_produces_valid_workbook:
    - Build ContaPlusData with journal=None, subcta=<SubctaTable with 1 row>,
      problems=ProblemsReport(entries=(ProblemEntry(table="DIARIO", row_index=-1,
      column="", reason="corrupt", value=""),))
    - Call render(data); assert isinstance(result, bytes) and len(result) > 0
    - Load with openpyxl.load_workbook(io.BytesIO(result))
    - Assert "Diario" in wb.sheetnames (sheet present even with no journal)
    - Assert wb["Diario"].max_row == 1 (only header row — no data rows)
    - Assert wb["Diario"]["A1"].value == "Fecha" (first header is correct)
    - Assert "Subcuentas" in wb.sheetnames
    - Assert "Problemas" in wb.sheetnames

    test_render_lenient_end_to_end:
    - Build a corrupt-DIARIO ZIP in-memory (using zipfile.ZipFile + io.BytesIO):
      - write "Emp01/Diario.dbf" with content b"NOT A DBF" (corrupt)
      - build a real SUBCTA.DBF using the dbf package (_build_subcta_dbf pattern from conftest)
        and write it as "Emp01/SubCta.dbf"
    - Call data = read(zip_bytes, lenient=True)
    - Assert data.journal is None
    - Assert data.subcta is not None
    - Call xlsx_bytes = render(data)  — MUST NOT raise
    - Assert isinstance(xlsx_bytes, bytes) and len(xlsx_bytes) > 0
    - Load with openpyxl.load_workbook(io.BytesIO(xlsx_bytes))
    - Assert "Diario" in wb.sheetnames
    - Assert "Subcuentas" in wb.sheetnames
    - Assert "Problemas" in wb.sheetnames
    - This test is the end-to-end proof of the D-03 contract at render time.
    - Use tmp_path fixture to build the SUBCTA.DBF on disk, then package into a BytesIO ZIP.
  </behavior>

  <action>Read xlsx.py in full before touching it. In render(), replace the unconditional
  _render_journal_sheet(wb.active, data.journal) call (line 87) with a guarded block:
  if data.journal is not None, call _render_journal_sheet as before; else call a new private
  helper _write_diario_headers_only(wb.active). Implement _write_diario_headers_only(ws) just
  below _render_journal_sheet: it sets ws.title = "Diario", writes HEADERS in row 1 with
  HEADER_FILL + HEADER_FONT per the same loop pattern used in _render_journal_sheet, sets
  ws.freeze_panes = "A2", and calls _autosize_columns(ws). Remove the type: ignore[arg-type]
  comment from the former line.

  In test_xlsx.py, add two new test functions at the bottom of the file. Both need:
  import io at the top of the file (if not already present), and imports from
  contaplus_reader.models (ContaPlusData, SubctaTable, SubctaRow, ProblemEntry, ProblemsReport).
  test_render_journal_none_produces_valid_workbook: construct a minimal SubctaTable with
  headers=("COD", "TITULO") and one SubctaRow(fields={"COD": "4300000", "TITULO": "Cliente"}),
  plus a ProblemsReport with one DIARIO ProblemEntry, pass both to ContaPlusData(journal=None,
  subcta=..., problems=...), call render(), load with openpyxl, assert Diario/Subcuentas/Problemas
  all in sheetnames and Diario max_row==1 and A1=="Fecha".
  test_render_lenient_end_to_end: uses tmp_path to write a SUBCTA.DBF (using dbf.Table with
  cod C(12)/titulo C(40)/nif C(15) spec), packages it with b"NOT A DBF" corrupt Diario into
  an in-memory ZIP (io.BytesIO), calls read(zip_bytes, lenient=True) then render(data), asserts
  no exception, valid bytes, sheetnames contain Diario/Subcuentas/Problemas.</action>

  <verify>
    <automated>uv run pytest tests/test_xlsx.py -x -q -k "render"</automated>
  </verify>

  <acceptance_criteria>
    - render(ContaPlusData(journal=None, subcta=..., problems=...)) returns non-empty bytes (no AttributeError)
    - Load the bytes with openpyxl.load_workbook: "Diario" in wb.sheetnames
    - wb["Diario"].max_row == 1 (headers only, no data rows)
    - wb["Diario"]["A1"].value == "Fecha" (first HEADERS entry)
    - "Subcuentas" in wb.sheetnames and "Problemas" in wb.sheetnames
    - xlsx.py contains "if data.journal is not None" (source assertion)
    - xlsx.py does NOT contain "type: ignore[arg-type]" on the journal render line
    - xlsx.py contains "def _write_diario_headers_only"
    - test_render_lenient_end_to_end: read(corrupt_diario_zip, lenient=True) then render(data) produces valid bytes with Diario/Subcuentas/Problemas sheets
    - uv run pytest tests/test_xlsx.py -x -q exits 0 (all xlsx tests green)
  </acceptance_criteria>

  <done>xlsx.py render() contains the journal None guard; _write_diario_headers_only exists;
  test_render_journal_none_produces_valid_workbook and test_render_lenient_end_to_end both pass;
  full test_xlsx.py suite remains green with no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-05 ValueError catch, canonicalize ProblemEntry.table casing, and fix CLI sheet count</name>
  <files>src/contaplus_reader/_reader.py, src/contaplus_reader/__init__.py, src/contaplus_reader/cli.py, tests/test_lenient.py</files>

  <read_first>
    src/contaplus_reader/_reader.py (lines 288-360 — the per-row loop and outer except)
    src/contaplus_reader/__init__.py (lines 62-99 _read_secondary_table; lines 170-255 D-03/D-13 blocks)
    src/contaplus_reader/cli.py (lines 123-137 — the sheet_count block)
    tests/test_lenient.py (lines 110-117 and 172-177 — assertions that use .lower()/.upper() workarounds)
  </read_first>

  <behavior>
    WR-05 fix in _reader.py (inner per-row except):
    - Current: except ContaPlusReadError as exc:
    - Fix: except (ContaPlusReadError, ValueError) as exc:
    - When exc is a ValueError (not ContaPlusReadError), it has no .row_index or .column attrs.
      Treat it as a per-row error: in lenient mode, append ProblemEntry(table="DIARIO",
      row_index=idx, column="", reason=str(exc), value="") and continue. In strict mode
      (lenient=False), re-raise the ValueError — it will be caught by the outer
      except (struct.error, ValueError, OSError) handler and wrapped as a file-level
      ContaPlusReadError, which is the existing strict behavior.
    - The isinstance check separates the two: if isinstance(exc, ValueError) and lenient:
      collect and continue; elif isinstance(exc, ValueError) and not lenient: raise;
      else (ContaPlusReadError path): existing logic unchanged.
    - The outer except (struct.error, ValueError, OSError) at lines 343-358 is not touched —
      it continues to handle file-level ValueError (raised during DBF open / _assert_journal_shaped,
      not during record iteration) correctly.

    New test in test_lenient.py (test_lenient_dbfread_valueerror_skipped):
    - Patch _build_journal_row to raise ValueError("dbfread field error") on idx==1.
    - Use unittest.mock.patch targeting contaplus_reader._reader._build_journal_row.
    - Build a real DIARIO.DBF with 3 rows using diario_with_bad_row or an inline builder.
    - Confirm lenient read skips idx==1, returns 2 rows, records one ProblemEntry with
      table=="DIARIO" and row_index==1.
    - Confirm strict mode re-raises (raises ContaPlusReadError or the raw ValueError —
      the test need only assert that strict raises, not the specific type).

    ProblemEntry.table canonical convention: uppercase stem, no extension.
    Examples: "DIARIO", "BALAN", "VENCI", "PREDE", "AMOINV", "NIVEL",
              "GRUPOS", "USUARIOS", "EMPRESA", "EXTRA" (for uncatalogued extra.dbf).
    This matches the existing "DIARIO" literal already used in _reader.py and __init__.py.

    Changes in __init__.py:
    - _read_secondary_table (lines 88-98): change table=table_name.lower() to
      table=Path(table_name).stem.upper()  (e.g. "grupos.dbf" -> "GRUPOS", "BALAN.DBF" -> "BALAN")
      Import Path is already at the top of __init__.py.
    - D-13 uncatalogued block (lines 247-255): change table=candidate.name.upper() to
      table=candidate.stem.upper()  (e.g. "extra.dbf" -> "EXTRA", not "EXTRA.DBF")
    - D-03 DIARIO blocks (lines 179-189 and 310-319): table="DIARIO" — already correct, no change.

    Update tests/test_lenient.py assertions:
    - Line 113: e.table.lower() == "venci.dbf" → e.table == "VENCI"
    - Line 176: e.table.upper() == "EXTRA.DBF" → e.table == "EXTRA"
    - No other assertions reference .table in test_lenient.py; the DIARIO assertions
      (e.table == "DIARIO") are already correct and require no change.

    CLI sheet count fix in cli.py:
    - Remove the manual sum() at lines 126-129 that counts only 5 tables.
    - Inside the main() function body, after xlsx_bytes is assigned, parse the workbook
      to count sheets: import io and openpyxl.load_workbook inside the function
      (consistent with the existing pattern of importing read and render inside main()).
    - sheet_count = len(load_workbook(io.BytesIO(xlsx_bytes), read_only=True, data_only=True).sheetnames)
    - Use read_only=True and data_only=True for minimal overhead — only sheetnames is needed.
    - The echo line uses sheet_count as before; row_count and skip_msg are unchanged.
  </behavior>

  <action>Read each target file in full before editing. Apply four separate changes:

  1. _reader.py: In the inner per-row except (lines ~296-313), change
  "except ContaPlusReadError as exc:" to "except (ContaPlusReadError, ValueError) as exc:".
  Add isinstance discrimination immediately inside the block: if isinstance(exc, ValueError)
  handle as a per-row ValueError (lenient: append ProblemEntry(table="DIARIO", row_index=idx,
  column="", reason=str(exc), value="") and continue; strict: raise); otherwise fall through
  to the existing ContaPlusReadError logic (check exc.row_index >= 0 for per-row vs file-level).
  Do not touch the outer except (struct.error, ValueError, OSError) handler.

  2. __init__.py: In _read_secondary_table, change ProblemEntry(table=table_name.lower(), ...)
  to ProblemEntry(table=Path(table_name).stem.upper(), ...). In the D-13 uncatalogued block,
  change ProblemEntry(table=candidate.name.upper(), ...) to
  ProblemEntry(table=candidate.stem.upper(), ...). Path is already imported at the top.
  Do not touch the D-03 DIARIO ProblemEntry lines — they already use the canonical "DIARIO".

  3. cli.py: Remove the sheet_count sum block (lines 126-129). Inside main(), after
  xlsx_bytes = render(data), add: import io as _io and from openpyxl import load_workbook
  as _load_wb (inside the function body to avoid polluting module namespace). Then:
  sheet_count = len(_load_wb(_io.BytesIO(xlsx_bytes), read_only=True, data_only=True).sheetnames).
  Keep row_count, skip_msg, problems_msg, and the typer.echo line unchanged.

  4. tests/test_lenient.py: Update two assertions — line 113 and line 176 — from
  .lower() == "venci.dbf" / .upper() == "EXTRA.DBF" to == "VENCI" / == "EXTRA" respectively.
  Add test_lenient_dbfread_valueerror_skipped: uses unittest.mock.patch on
  contaplus_reader._reader._build_journal_row to raise ValueError("simulated dbfread error")
  on call number 2 (idx==1); passes diario_with_bad_row as the test input in lenient mode;
  asserts 2 rows returned, 1 ProblemEntry with table=="DIARIO" and row_index==1;
  also tests strict mode raises (pytest.raises).</action>

  <verify>
    <automated>uv run pytest tests/test_lenient.py tests/test_cli.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - _reader.py inner except contains "(ContaPlusReadError, ValueError)" (source assertion)
    - __init__.py _read_secondary_table ProblemEntry uses Path(table_name).stem.upper() (source assertion)
    - __init__.py D-13 block uses candidate.stem.upper() (source assertion)
    - test_lenient_dbfread_valueerror_skipped: lenient read with patched _build_journal_row raising
      ValueError returns 2 rows and 1 ProblemEntry(table="DIARIO", row_index=1)
    - test_lenient_corrupt_table: entry.table == "VENCI" (no .lower() workaround)
    - test_lenient_uncatalogued_dbf: matching[0].table == "EXTRA" (no .upper() workaround)
    - cli.py sheet_count line contains "load_workbook" (source assertion)
    - CLI test for a full Phase 3 ZIP (zip_with_all_tables fixture): reported sheet count
      matches len(wb.sheetnames) from the same rendered bytes
    - uv run pytest tests/ -x -q exits 0 (full suite green, no regressions)
  </acceptance_criteria>

  <done>All four warning fixes applied; test_lenient.py updated assertions pass without
  .lower()/.upper() workarounds; new WR-05 test passes; CLI sheet_count is derived from
  the rendered workbook; full 120+ test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Corrupt ContaPlus archive → lenient reader | Attacker-controlled bytes enter via _reader.py per-row loop; catching ValueError broadens the catch surface |
| Corrupt archive → render() | render() receives ContaPlusData with journal=None from lenient read; the None guard must not introduce new crash paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Denial of Service | _reader.py catching ValueError broadly | mitigate | ValueError catch is scoped to the inner per-row try block only; file-level ValueError (raised during DBF open or _assert_journal_shaped) still escapes to the outer handler and becomes a structured ContaPlusReadError with row_index=-1; lenient mode only swallows per-row ValueErrors, it cannot suppress a whole-file failure silently |
| T-03-18 | Denial of Service | render() with journal=None iterating None.rows | mitigate | The None guard (if data.journal is not None) prevents the AttributeError; _write_diario_headers_only writes only the fixed HEADERS tuple — no user-controlled data iterates; no new attack surface |
| T-03-19 | Tampering | ProblemEntry.table casing normalization | accept | Casing normalization is purely cosmetic (uppercase stem); it does not affect validation logic, error handling, or data extraction; consumers filtering by table name will receive consistent values |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this gap-closure plan; io and openpyxl are already runtime deps |
</threat_model>

<verification>
After both tasks:
- uv run pytest tests/ -v — full suite GREEN (120+ tests, no regressions)
- uv run pytest tests/test_xlsx.py::test_render_journal_none_produces_valid_workbook tests/test_xlsx.py::test_render_lenient_end_to_end -v — both GREEN
- uv run pytest tests/test_lenient.py::test_lenient_dbfread_valueerror_skipped -v — GREEN
- uv run pytest tests/test_lenient.py::test_lenient_corrupt_table tests/test_lenient.py::test_lenient_uncatalogued_dbf -v — GREEN (updated assertions)
- Source check: grep "if data.journal is not None" src/contaplus_reader/xlsx.py — matches
- Source check: grep "ContaPlusReadError, ValueError" src/contaplus_reader/_reader.py — matches
- Source check: grep "stem.upper" src/contaplus_reader/__init__.py — matches both call sites
- Source check: grep "load_workbook" src/contaplus_reader/cli.py — matches
</verification>

<success_criteria>
- render() never crashes with AttributeError when data.journal is None; produces a headers-only Diario sheet
- D-03 end-to-end: lenient read() of corrupt-DIARIO ZIP followed by render() produces valid workbook bytes
- In lenient mode, a dbfread ValueError during per-row iteration skips only that row and appends ProblemEntry; in strict mode the ValueError propagates to the outer file-level handler
- ProblemEntry.table is always an uppercase stem (no extension) at every call site in __init__.py and _reader.py
- CLI sheet count matches the actual number of sheets in the produced workbook, regardless of which tables are present
- Full test suite (120+ tests) GREEN with zero regressions
</success_criteria>

<output>
Create `.planning/phases/03-full-tables-balance-lenient-path/03-05-SUMMARY.md` when done
</output>
