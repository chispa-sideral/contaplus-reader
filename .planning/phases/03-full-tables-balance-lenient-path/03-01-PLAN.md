---
phase: 03
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_tables.py
  - tests/test_balance.py
  - tests/test_lenient.py
  - tests/test_xlsx.py
autonomous: true
requirements:
  - TABL-03
  - TABL-04
  - BAL-01
  - BAL-02
  - API-03
  - XLSX-02
  - XLSX-03

must_haves:
  truths:
    - "All Phase 3 test files exist with failing tests for every required behavior"
    - "Synthetic fixtures for venci/prede/amoinv/nivel/balan DBFs exist in conftest.py (D-14: schemas validated against pii-test-data/ before readers committed; committed tests use synthetic blob-free fixtures only)"
    - "Defect-bearing fixtures for lenient path tests (bad journal row, corrupt secondary table) exist in conftest.py"
    - "Test for unbalanced BALAN fixture that triggers descuadre banner exists"
    - "Full test suite runs and existing Phase 1/2 tests remain green"
  artifacts:
    - path: "tests/test_tables.py"
      provides: "TABL-03 / BAL-01 table reader tests (failing)"
      exports: ["test_venci_headers", "test_nivel_row_count", "test_balan_sdo_cierre", "test_prede_headers", "test_amoinv_headers"]
    - path: "tests/test_balance.py"
      provides: "BAL-02 trial-balance computation tests (failing)"
      exports: ["test_decimal_precision", "test_saldo_deudor_acreedor", "test_by_subcuenta_descripcion"]
    - path: "tests/test_lenient.py"
      provides: "API-03 lenient path tests (failing)"
      exports: ["test_lenient_bad_journal_row_skipped", "test_strict_still_raises_on_bad_row", "test_lenient_corrupt_table", "test_lenient_corrupt_journal", "test_lenient_uncatalogued_dbf"]
    - path: "tests/test_xlsx.py"
      provides: "XLSX-02 / XLSX-03 renderer tests (failing — new functions only)"
      contains: "test_problems_sheet_present"
    - path: "tests/conftest.py"
      provides: "Synthetic fixtures for all new tables and defect-bearing DBFs"
      contains: "_VENCI_SPEC"
  key_links:
    - from: "tests/test_tables.py"
      to: "contaplus_reader._subcta.read_table_raw"
      via: "import at module level"
      pattern: "from contaplus_reader._subcta import read_table_raw"
    - from: "tests/test_balance.py"
      to: "contaplus_reader._balance.compute_balance"
      via: "import at module level"
      pattern: "from contaplus_reader._balance import compute_balance"
    - from: "tests/test_lenient.py"
      to: "contaplus_reader.read"
      via: "import at module level"
      pattern: "from contaplus_reader import read"
---

## Phase Goal

**As a** ContaPlus migrating user, **I want to** run a lenient conversion and get every readable table extracted — including operational tables and a recomputed trial balance — plus a problems sheet listing anything that was skipped, **so that** I can migrate my accounting data even when the archive has defects.

<objective>
Create the complete test scaffold for Phase 3 — all failing tests and conftest fixtures that define the acceptance contract for every new Phase 3 behavior before any implementation begins.

Purpose: TDD Wave 0 — failing tests define what "done" means for every requirement. Implementation in Plans 02-04 turns them green.
Output: 4 new/extended test files with specific failing test functions; conftest.py extended with synthetic fixtures for venci/prede/amoinv/nivel/balan DBFs and defect-bearing fixtures for lenient path tests.
</objective>

<execution_context>
@C:/dev/a3api/.claude/gsd/plugins/gsd/get-shit-done/workflows/execute-plan.md
@C:/dev/a3api/.claude/gsd/plugins/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-full-tables-balance-lenient-path/03-CONTEXT.md
@.planning/phases/03-full-tables-balance-lenient-path/03-RESEARCH.md
@.planning/phases/03-full-tables-balance-lenient-path/03-PATTERNS.md

<interfaces>
<!-- Existing conftest.py patterns the executor must replicate -->

From tests/conftest.py (relevant patterns):
- `_build_generic_dbf(path, spec_str, rows)` — builds a synthetic GenericTable-compatible DBF from a spec string and row dicts; reuse for venci/prede/amoinv/nivel/balan
- `_build_diario_dbf(path, rows, codepage)` — builds a synthetic DIARIO.DBF; reuse for defect-bearing fixtures
- `scope="session"` on all fixtures — DBF builds are expensive, session scope amortises cost
- DBF spec string format: `"FIELD1 TYPE(width); FIELD2 TYPE(width,dec); ..."` parsed by _build_generic_dbf

From tests/test_reader.py (structure pattern):
- Module docstring listing covered requirements
- `from __future__ import annotations`
- `from contaplus_reader import ContaPlusReadError, read`
- `from contaplus_reader.models import ContaPlusData`
- One assertion per test function; descriptive test names

Existing test_xlsx.py import block uses:
```python
import io
from openpyxl import load_workbook
from contaplus_reader.xlsx import render
from contaplus_reader.models import ContaPlusData
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend conftest.py with Phase 3 synthetic fixtures</name>
  <files>tests/conftest.py</files>
  <read_first>
    tests/conftest.py
    .planning/phases/03-full-tables-balance-lenient-path/03-RESEARCH.md
    .planning/phases/03-full-tables-balance-lenient-path/03-PATTERNS.md
  </read_first>
  <behavior>
    - _VENCI_SPEC constant contains all 21 fields in the exact order from RESEARCH.md §Q1: FECHA D; COD C(12); ACPA C(1); CONTRA C(12); CONCEPTO C(25); PTA N(16,2); TIPO C(2); PREPROCESO L; ESTADO L; DOCUMENTO C(10); IMPMONEX N(16,2); CODDIVISA C(5); MONEDAUSO C(1); LFACTPLUS L; FECHAPAG D; REMESA C(12); NCOBRO N(2,0); NPAGARE N(2,0); EURO N(16,2); NUMRECFAC C(12); METALIMP N(16,2)
    - _PREDE_SPEC constant contains the full 41-field spec from RESEARCH.md §Q1
    - _AMOINV_SPEC constant contains the full 35-field spec from RESEARCH.md §Q1
    - _NIVEL_SPEC constant: N1 L; N2 L; N3 L; N4 L; N5 L; N6 L; N7 L; N8 L; N9 L; N10 L; N11 L; N12 L; LASTCASADO N(6,0)
    - _BALAN_SPEC constant: NATURALEZA C(2); CODBAL C(10); DESCRIP C(100); CTA C(11); TIPO N(1,0); BITMAP C(10); DOBLE C(1); FORMULA C(255); NIVEL N(2,0); DESGLOSE N(2,0); ACPA C(1); NUMERO C(6); CTAPGC C(12); SDO_CIERRE N(19,2); NIV_CIERRE N(2,0); LINTERRUMP L; NOTMEMORIA C(50)
    - venci_dbf fixture: session scope; empty rows (0 rows, matching real data); path returned
    - prede_dbf fixture: session scope; empty rows (0 rows, matching real data); path returned
    - amoinv_dbf fixture: session scope; empty rows (0 rows, matching real data); path returned
    - nivel_dbf fixture: session scope; exactly 1 row (N1=True, N2=True, N3=False, N4=False, N5=False, N6=False, N7=False, N8=False, N9=False, N10=False, N11=False, N12=False, LASTCASADO=0); path returned
    - balan_balanced_dbf fixture: session scope; 3 rows with SDO_CIERRE values that sum to 0.00 (e.g. 100.00, -60.00, -40.00); path returned
    - balan_unbalanced_dbf fixture: session scope; 3 rows with SDO_CIERRE values that sum to non-zero (e.g. 100.00, -60.00, -30.00, sum=10.00); path returned
    - diario_with_bad_row fixture: session scope; DIARIO.DBF with 3 rows — row 0 (index 0): valid (subcta="4300000", eurodebe=100.0, eurohaber=0.0, fecha=2025-01-01, asien=1); row 1 (index 1): bad subcta="BADCODE" (triggers D-E3 malformed subcuenta); row 2 (index 2): valid (subcta="7000000", eurodebe=0.0, eurohaber=100.0, fecha=2025-01-03, asien=3); path returned
    - zip_with_all_tables fixture: session scope; ZIP archive containing all 10 catalogued tables (DIARIO.DBF plus SUBCTA.DBF plus BALAN.DBF plus grupos.dbf plus usuarios.dbf plus empresa.dbf plus venci.dbf plus prede.dbf plus amoinv.dbf plus nivel.dbf) under a single company prefix (e.g. "Emp01/"); path returned; use the existing cp850_basic_dbf for DIARIO, existing group/subcta/empresa fixtures, and the new synthetic tables for the rest
    - zip_with_uncatalogued fixture: session scope; ZIP containing DIARIO.DBF plus at least one unrecognized .dbf file (e.g. "Emp01/extra.dbf"); path returned; "extra.dbf" has a minimal 1-field spec "CAMPO C(10)" with 0 rows
  </behavior>
  <action>Extend tests/conftest.py — append after the last existing Phase 2 fixture. Add the five spec constants (_VENCI_SPEC, _PREDE_SPEC, _AMOINV_SPEC, _NIVEL_SPEC, _BALAN_SPEC) verbatim from RESEARCH.md §Q1 and §Q2 synthetic spec tables. Add nine new session-scoped fixtures: venci_dbf, prede_dbf, amoinv_dbf, nivel_dbf (1 row per RESEARCH.md Pitfall 6), balan_balanced_dbf, balan_unbalanced_dbf, diario_with_bad_row (3-row DIARIO with row index 1 having subcta="BADCODE"), zip_with_all_tables (10-table ZIP under Emp01/ prefix), and zip_with_uncatalogued (DIARIO + "extra.dbf"). Use the existing _build_generic_dbf and _build_diario_dbf helpers. All fixtures use tmp_path_factory for session scope. Per PATTERNS.md, the uncatalogued ZIP's extra.dbf must have suffix .dbf and a name not in the 10-table catalogue.</action>
  <verify>
    <automated>uv run pytest tests/conftest.py --collect-only -q 2>&1 | head -5</automated>
  </verify>
  <done>conftest.py contains _VENCI_SPEC through _BALAN_SPEC constants; nine new fixtures are importable; `uv run pytest tests/ -x -q` keeps all existing Phase 1/2 tests green (no regressions)</done>
</task>

<task type="tdd">
  <name>Task 2: Write failing tests — test_tables.py, test_balance.py, test_lenient.py, test_xlsx.py extensions</name>
  <files>tests/test_tables.py, tests/test_balance.py, tests/test_lenient.py, tests/test_xlsx.py</files>
  <read_first>
    tests/conftest.py
    tests/test_reader.py
    tests/test_xlsx.py
    .planning/phases/03-full-tables-balance-lenient-path/03-RESEARCH.md
    .planning/phases/03-full-tables-balance-lenient-path/03-PATTERNS.md
  </read_first>
  <behavior>
    tests/test_tables.py (new file, covers TABL-03 + BAL-01):
    - test_venci_returns_generic_table: read_table_raw(venci_dbf, "venci.dbf") returns GenericTable; isinstance check passes
    - test_venci_headers: table.headers[0] == "FECHA", table.headers[1] == "COD" (exact field order from RESEARCH.md §Q1)
    - test_venci_zero_rows: len(table.rows) == 0 (matching real data — 0 rows in all archives)
    - test_prede_returns_generic_table: read_table_raw(prede_dbf, "prede.dbf") returns GenericTable
    - test_prede_headers: table.headers[0] == "ASIEN", table.headers[1] == "TSUBCTA"
    - test_amoinv_returns_generic_table: read_table_raw(amoinv_dbf, "amoinv.dbf") returns GenericTable
    - test_amoinv_headers: table.headers[0] == "NUMEROINV", table.headers[1] == "DOCUMENTO"
    - test_nivel_returns_generic_table: read_table_raw(nivel_dbf, "nivel.dbf") returns GenericTable
    - test_nivel_row_count: len(table.rows) == 1 (always exactly 1 row per RESEARCH.md Pitfall 6)
    - test_nivel_headers: table.headers[0] == "N1", table.headers[-1] == "LASTCASADO"
    - test_balan_returns_generic_table: read_table_raw(balan_balanced_dbf, "BALAN.DBF") returns GenericTable
    - test_balan_sdo_cierre_column: "SDO_CIERRE" in table.headers (BAL-01 descuadre check requirement)

    tests/test_balance.py (new file, covers BAL-02):
    - test_decimal_precision: rows=[_make_row("4300000", 0.1, 0.0), _make_row("4300000", 0.2, 0.0)]; compute_balance returns table.rows[0].suma_debe == Decimal("0.3") (not float 0.30000000000000004)
    - test_saldo_deudor_when_debe_exceeds_haber: rows with cuenta "1000" having suma_debe=Decimal("100"), suma_haber=Decimal("60") → saldo_deudor=Decimal("40"), saldo_acreedor=Decimal("0"), saldo=Decimal("40")
    - test_saldo_acreedor_when_haber_exceeds_debe: rows with suma_haber > suma_debe → saldo_acreedor=positive, saldo_deudor=Decimal("0"), saldo=negative
    - test_cuenta_level_grouping: two subcuentas "4300000" and "4300001" both group to cuenta "4300" in by_subcuenta=False mode → one BalanceRow with code="4300"
    - test_subcuenta_level_grouping: same rows in by_subcuenta=True mode → two BalanceRows with code="4300000" and code="4300001"
    - test_by_subcuenta_descripcion: when subcta_lookup={"4300000": "Clientes"} is passed and by_subcuenta=True → BalanceRow.descripcion == "Clientes"
    - test_returns_balance_table_type: compute_balance returns BalanceTable; table.level == "cuenta" when by_subcuenta=False; table.level == "subcuenta" when by_subcuenta=True
    - test_sorted_output: BalanceRows are sorted ascending by code

    tests/test_lenient.py (new file, covers API-03):
    - test_lenient_bad_journal_row_skipped: read(diario_with_bad_row.read_bytes(), lenient=True) returns data with data.journal.rows having 2 rows (bad row skipped); data.problems is not None; len(data.problems.entries) == 1; entry.table == "DIARIO"; entry.row_index == 1; entry.column is non-empty string
    - test_lenient_good_rows_preserved: same call — data.journal.rows[0].debe == 100.0 (first good row preserved)
    - test_strict_still_raises_on_bad_row: read(diario_with_bad_row.read_bytes()) (no lenient) raises ContaPlusReadError with row_index == 1
    - test_lenient_corrupt_table: build a ZIP inline (via tmp_path) with a valid DIARIO.DBF but a corrupt venci.dbf (e.g. write b"NOT A DBF" as content); read(zip_bytes, lenient=True) returns data with data.venci is None; data.problems is not None; any(e.table.lower() == "venci.dbf" and e.row_index == -1 for e in data.problems.entries)
    - test_lenient_corrupt_journal: build a ZIP inline with a corrupt DIARIO.DBF (write b"NOT A DBF") and a valid SUBCTA.DBF; read(zip_bytes, lenient=True) returns data with data.journal is None; data.problems is not None; any(e.table == "DIARIO" and e.row_index == -1 for e in data.problems.entries); data.subcta is not None (conversion continued)
    - test_lenient_uncatalogued_dbf: read(zip_with_uncatalogued.read_bytes(), lenient=True) returns data with data.problems entries containing one entry where entry.table.upper() == "EXTRA.DBF" and entry.row_index == -1 and "unrecognized" in entry.reason.lower()
    - test_memo_skips_not_in_problems: build DIARIO with one both-zero row; read(bytes, lenient=True) returns data.problems is None or len(data.problems.entries) == 0; and data.journal.skipped_memo == 1
    - test_no_problems_when_clean: read(clean_zip, lenient=True) returns data.problems is None

    tests/test_xlsx.py (extend — append new test functions, do not modify existing ones):
    - test_full_zip_all_tables_sheets_present: render(data_with_all_tables) returns workbook with sheet titles "Vencimientos", "Predefinidos", "Amortizaciones", "Niveles" present
    - test_problems_sheet_present: render(data_with_problems) produces workbook with a sheet named "Problemas"
    - test_problems_sheet_columns: Problemas sheet row 1 has headers "Tabla", "Fila", "Columna", "Motivo", "Valor" in columns A-E
    - test_problems_sheet_has_entries: Problemas sheet has at least one data row (row 2) with entry.table value in column A
    - test_memo_not_in_problems: render(data_where_journal_has_skipped_memo_but_no_problems) — no "Problemas" sheet present (memo skips do not appear, per D-05)
    - test_balan_no_banner_when_balanced: render(data_with_balanced_balan) — "Balance" sheet row 1 is a header row (cell A1 value is a field name from _BALAN_SPEC headers, not a warning string)
    - test_balan_banner_on_descuadre: render(data_with_unbalanced_balan) — "Balance" sheet row 1 cell A1 contains the string "AVISO" (banner injected before headers)
    - test_balance_cuenta_sheet_present: render(data_with_balance) produces sheet "Sumas y Saldos (Cuentas)"
    - test_balance_subcuenta_sheet_present: render(data_with_balance) produces sheet "Sumas y Saldos (Subcuentas)"
    - test_balance_subcuenta_descripcion: subcuenta-level sheet has "Descripción" as column B header
    - test_balance_cuenta_columns: cuenta-level sheet row 1 has headers "Cuenta", "Suma Debe", "Suma Haber", "Saldo Deudor", "Saldo Acreedor", "Saldo" in A1-F1
  </behavior>
  <action>Create tests/test_tables.py, tests/test_balance.py, tests/test_lenient.py as new files. Extend tests/test_xlsx.py by appending the new test functions after the last existing test. All test functions import only from contaplus_reader (the modules they test will not exist yet — tests will fail with ImportError or AttributeError, which counts as RED). Use the fixture names defined in Task 1. In test_balance.py use a local _make_row helper that constructs JournalRow directly. In test_lenient.py the test_lenient_corrupt_table and test_lenient_corrupt_journal tests build their ZIPs inline using zipfile and a tmp_path fixture parameter. In test_xlsx.py use local helper functions (e.g. _make_data_with_problems, _make_data_with_all_tables) that construct ContaPlusData instances directly (setting attributes to None or to simple stubs where the models don't exist yet — tests will fail at import time, which is expected RED). Follow the `from __future__ import annotations` and module docstring pattern from test_reader.py.</action>
  <verify>
    <automated>uv run pytest tests/test_tables.py tests/test_balance.py tests/test_lenient.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All new test files are syntactically valid Python and importable; `uv run pytest tests/ -q --tb=no` shows new test functions collected; existing Phase 1/2 tests still pass; new Phase 3 tests fail (RED — ImportError or AttributeError on the not-yet-implemented symbols). Full suite command `uv run pytest tests/ -x -q` exits non-zero only because of the new failing tests, not because of regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → conftest fixtures | Synthetic DBF data is generated in-process; no untrusted input at this stage |
| conftest → dbf (ethanfurman) writer | dbf package writes synthetic DBFs; no untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | conftest.py fixture DBF specs | accept | Specs are constant strings derived from RESEARCH.md verified schemas; no external input |
| T-03-02 | Tampering | zip_with_uncatalogued fixture | accept | ZIP is built in-process; no path traversal possible since arcnames are hardcoded strings |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 3 — zero install tasks in this plan |
</threat_model>

<verification>
After both tasks:
- `uv run pytest tests/ -q --tb=no` — existing 40+ Phase 1/2 tests pass; new Phase 3 tests collected and RED
- `uv run pytest tests/test_tables.py tests/test_balance.py tests/test_lenient.py -q --tb=line` — all new tests collected; failures are ImportError / AttributeError (not syntax errors or fixture errors)
- `uv run pytest tests/conftest.py --collect-only` — no collection errors
</verification>

<success_criteria>
- tests/conftest.py contains _VENCI_SPEC, _PREDE_SPEC, _AMOINV_SPEC, _NIVEL_SPEC, _BALAN_SPEC constants plus nine new session-scoped fixtures
- tests/test_tables.py exists with 12 test functions covering TABL-03 and BAL-01
- tests/test_balance.py exists with 8 test functions covering BAL-02
- tests/test_lenient.py exists with 8 test functions covering API-03 (D-02, D-03, D-04, D-05, D-13)
- tests/test_xlsx.py has 11 new test functions covering XLSX-02 and XLSX-03
- All existing Phase 1/2 tests remain green (no regressions from conftest extension)
- New tests are RED (failing) — providing the acceptance contract for Plans 02-04
</success_criteria>

<output>
Create `.planning/phases/03-full-tables-balance-lenient-path/03-01-SUMMARY.md` when done
</output>
