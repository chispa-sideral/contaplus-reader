---
phase: 01-journal-slice
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/contaplus_reader/_sniffer.py
  - src/contaplus_reader/_reader.py
  - src/contaplus_reader/models.py
  - src/contaplus_reader/cli.py
  - src/contaplus_reader/xlsx.py
  - tests/test_reader.py
  - tests/test_cli.py
  - tests/test_xlsx.py
autonomous: true
requirements:
  - INPUT-03
  - JRNL-03
  - API-02
  - CLI-01
gap_closure: true

must_haves:
  truths:
    - "A row with both-non-zero debe/haber (including mixed-sign, e.g. debe=-10.0, haber=50.0) raises ContaPlusReadError with row_index>=0"
    - "CLI exits with code 1 and emits a Rich error panel (no 'Traceback' in output) when given a non-existent input file"
    - "ContaPlusJournal.rows is tuple[JournalRow, ...] — journal.rows.append() raises AttributeError"
    - "read(non_seekable_BinaryIO) raises ContaPlusReadError (not raw OSError)"
  artifacts:
    - path: "src/contaplus_reader/_reader.py"
      provides: "D-C2 guard using != 0"
      contains: "if debe != 0 and haber != 0"
    - path: "src/contaplus_reader/_sniffer.py"
      provides: "Seekable-safe BinaryIO check"
      contains: "data.seekable()"
    - path: "src/contaplus_reader/models.py"
      provides: "Immutable rows sequence on ContaPlusJournal"
      contains: "rows: tuple[JournalRow, ...]"
    - path: "src/contaplus_reader/cli.py"
      provides: "OSError guard around read_bytes()"
      contains: "except OSError"
    - path: "tests/test_reader.py"
      provides: "Tests for CR-04 and CR-01"
      exports: ["test_negative_debe_positive_haber_raises", "test_non_seekable_binary_io_raises_structured_error"]
    - path: "tests/test_cli.py"
      provides: "Test for CR-02"
      exports: ["test_cli_nonexistent_input_exits_nonzero"]
    - path: "tests/test_xlsx.py"
      provides: "Updated _make_journal helper using tuple"
      contains: "rows=rows"
  key_links:
    - from: "src/contaplus_reader/_reader.py"
      to: "src/contaplus_reader/models.py"
      via: "ContaPlusJournal(rows=tuple(rows), ...)"
      pattern: "rows=tuple\\(rows\\)"
    - from: "src/contaplus_reader/_sniffer.py"
      to: "src/contaplus_reader/models.py"
      via: "ContaPlusReadError raised on non-seekable stream"
      pattern: "data\\.seekable\\(\\)"
    - from: "src/contaplus_reader/cli.py"
      to: "rich.panel.Panel"
      via: "OSError handler prints error panel before typer.Exit(1)"
      pattern: "except OSError"
---

<objective>
Close the four blockers identified in VERIFICATION.md that caused requirements INPUT-03, JRNL-03, API-02, and CLI-01 to remain PARTIAL after phase execution.

Purpose: The happy-path conversion pipeline is fully functional (confirmed by human checkpoint in Plan 03). These four fixes address error-handling correctness and the data-integrity contract for ContaPlusJournal.rows — all of which matter for the downstream tax-workbench consumer and for any user encountering an invalid input.

Output: All four VERIFICATION.md "failed" truths become passing; the pytest suite grows from 50 to 54+ tests; `uv run pytest` exits 0.
</objective>

<execution_context>
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/workflows/execute-plan.md
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/dev/contaplus-reader/.planning/ROADMAP.md
@C:/dev/contaplus-reader/.planning/phases/01-journal-slice/01-VERIFICATION.md
@C:/dev/contaplus-reader/.planning/phases/01-journal-slice/01-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts extracted from the live codebase. -->
<!-- Executor reads <read_first> files for full context; these are navigation anchors. -->

From src/contaplus_reader/models.py (line 110-121):
  @dataclass(frozen=True)
  class ContaPlusJournal:
      rows: list[JournalRow]        # CR-03 target — change to tuple[JournalRow, ...]
      skipped_memo: int = 0
      source_name: str | None = None

From src/contaplus_reader/_sniffer.py (lines 46-48):
  first_byte_bytes = data.read(1)
  if hasattr(data, "seek"):         # CR-01 target — change to seekable() check
      data.seek(0)

From src/contaplus_reader/_reader.py (line 157):
  if debe > 0 and haber > 0:        # CR-04 target — change to != 0

From src/contaplus_reader/cli.py (lines 52-53):
  try:
      data = read(input_file.read_bytes(), source_name=str(input_file))
      # CR-02 target: wrap read_bytes() in try/except OSError before the existing try block

From src/contaplus_reader/_reader.py (line 211-215):
  return ContaPlusJournal(
      rows=rows,                    # must become rows=tuple(rows) after CR-03 fix
      skipped_memo=skipped_memo,
      source_name=source_name,
  )

From tests/test_xlsx.py (line 41-43):
  def _make_journal(*rows: JournalRow) -> ContaPlusJournal:
      return ContaPlusJournal(rows=list(rows), skipped_memo=0)
      # must become rows=tuple(rows) after CR-03 fix

From tests/conftest.py (line 160-172) — diario_dbf_builder fixture signature:
  def diario_dbf_builder(tmp_path_factory) -> Callable[[list[dict[str, Any]]], Path]
  # rows dict keys for DIARIO.DBF: asien, fecha, subcta, contra, concepto, eurodebe, eurohaber
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-04 — Fix mixed-sign both-non-zero guard and add test</name>
  <files>src/contaplus_reader/_reader.py, tests/test_reader.py</files>
  <read_first>
    - src/contaplus_reader/_reader.py — read in full; locate line 157 (`if debe > 0 and haber > 0`) and the comment above it ("NOTE: uses > 0, NOT != 0 — Pitfall 2"); also read the existing `test_negative_debe_passes_through` test in tests/test_reader.py to understand which fixture dict keys diario_dbf_builder accepts
    - tests/test_reader.py — read in full to understand existing test structure, imports, and the diario_dbf_builder fixture usage pattern (look at test_negative_debe_passes_through around line 200)
  </read_first>
  <behavior>
    - test_negative_debe_positive_haber_raises: build a DBF with eurodebe=-10.0, eurohaber=50.0; call read(bytes); assert pytest.raises(ContaPlusReadError); assert exc.row_index >= 0
    - existing test_negative_debe_passes_through (eurodebe=-1.0, eurohaber=0.0): must still pass — one side is zero so != 0 check does not trigger
    - existing test_negative_haber_passes_through (eurodebe=0.0, eurohaber=-52.56): must still pass
  </behavior>
  <action>
    In src/contaplus_reader/_reader.py at line 157: change `if debe > 0 and haber > 0:` to `if debe != 0 and haber != 0:`. Update the comment on the line above (line 156) to read: "D-C2: both-non-zero (any sign) is non-standard -- raise. D-C1 negatives (-10, 0) or (0, -50) still pass; (-10, 50) does not." Remove the old "NOTE: uses > 0, NOT != 0 — Pitfall 2" comment — it documented intentional wrong behavior.

    In tests/test_reader.py: add test `test_negative_debe_positive_haber_raises` after the existing `test_negative_haber_passes_through` test. The test uses the `diario_dbf_builder` fixture to build one row with eurodebe=-10.0 and eurohaber=50.0 (all other fields same shape as test_negative_debe_passes_through). Assert `pytest.raises(ContaPlusReadError) as exc_info`; assert `exc_info.value.row_index >= 0`. The test docstring: "D-C2: row with negative debe AND positive haber is still both-non-zero — must raise."
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_reader.py::test_negative_debe_positive_haber_raises tests/test_reader.py::test_negative_debe_passes_through tests/test_reader.py::test_negative_haber_passes_through -v</automated>
  </verify>
  <acceptance_criteria>
    - _reader.py line 157 reads exactly: `if debe != 0 and haber != 0:`
    - test_negative_debe_positive_haber_raises exists in test_reader.py and exits 0
    - test_negative_debe_passes_through still exits 0 (one-side-zero rows unaffected)
    - test_negative_haber_passes_through still exits 0
    - `uv run pytest tests/test_reader.py -v` shows all reader tests passing
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-01 — Fix non-seekable BinaryIO crash in sniffer and add test</name>
  <files>src/contaplus_reader/_sniffer.py, tests/test_reader.py</files>
  <read_first>
    - src/contaplus_reader/_sniffer.py — read in full; locate line 47 (`if hasattr(data, "seek")`); understand the full sniff() function structure and what ContaPlusReadError fields it uses at line 57-61
    - src/contaplus_reader/models.py — read ContaPlusReadError definition to confirm constructor signature (row_index, column, message)
  </read_first>
  <behavior>
    - test_non_seekable_binary_io_raises_structured_error: wrap valid DBF bytes in a non-seekable BinaryIO (use io.RawIOBase subclass or io.BytesIO wrapped in a class that overrides seekable() to return False and seek() to raise OSError); call read(stream); assert pytest.raises(ContaPlusReadError) with message containing "seekable" or "not seekable"; assert NOT pytest.raises(OSError) propagating raw
    - existing test_read_accepts_binary_io (uses open() on a real seekable file): must still pass
  </behavior>
  <action>
    In src/contaplus_reader/_sniffer.py: replace the block at lines 46-48:
      `first_byte_bytes = data.read(1)`
      `if hasattr(data, "seek"):`
      `    data.seek(0)`
    with the following logic (prose, not code block):
    Read 1 byte from the stream. Then check seekability using `hasattr(data, "seekable") and data.seekable()` — if True, call `data.seek(0)`. If the stream has `seekable` but `data.seekable()` is False (a non-seekable stream), raise `ContaPlusReadError(row_index=-1, column=None, message="Input stream is not seekable; provide bytes or a seekable BinaryIO")`. As a safety fallback, if no `seekable` attribute exists, attempt `data.seek(0)` inside a `try/except OSError` that re-raises as `ContaPlusReadError` with the same message. Keep the rest of the function (the first_byte check against _KNOWN_DBF_VERSION_BYTES) unchanged.

    In tests/test_reader.py: add test `test_non_seekable_binary_io_raises_structured_error` after the existing `test_read_accepts_binary_io` test. Create a helper class inside the test (or at module level) — a subclass of `io.RawIOBase` that wraps bytes, overrides `read(n=-1)` to return the bytes, overrides `seekable()` to return `False`, and overrides `seek()` to raise `OSError("Illegal seek")`. Construct this class with valid DBF bytes from the `cp850_basic_dbf` fixture. Call `read(non_seekable_stream)` inside `pytest.raises(ContaPlusReadError)`. Confirm `ContaPlusReadError` is raised rather than a bare `OSError`.
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_reader.py::test_non_seekable_binary_io_raises_structured_error tests/test_reader.py::test_read_accepts_binary_io -v</automated>
  </verify>
  <acceptance_criteria>
    - _sniffer.py line 47 no longer contains `hasattr(data, "seek")` as the seekable check; the new condition uses `data.seekable()`
    - test_non_seekable_binary_io_raises_structured_error exists in test_reader.py and exits 0
    - test_read_accepts_binary_io still exits 0 (seekable file-like objects unaffected)
    - `grep -n "hasattr(data, \"seek\")" src/contaplus_reader/_sniffer.py` returns no output (old check removed)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-03 — Make ContaPlusJournal.rows immutable (tuple) and update callers</name>
  <files>src/contaplus_reader/models.py, src/contaplus_reader/_reader.py, tests/test_xlsx.py, tests/test_reader.py</files>
  <read_first>
    - src/contaplus_reader/models.py — read ContaPlusJournal definition (lines 110-121); note the existing `rows: list[JournalRow]` field
    - src/contaplus_reader/_reader.py — read the return statement at line 211-215 (`return ContaPlusJournal(rows=rows, ...)`) and the `rows: list[JournalRow] = []` accumulator setup earlier in _read_dbf_path
    - tests/test_xlsx.py — read _make_journal helper (lines 41-43): `ContaPlusJournal(rows=list(rows), skipped_memo=0)`; this is the only caller passing rows as a list literal
    - tests/test_reader.py — grep for any other call sites constructing ContaPlusJournal directly (to ensure none are missed)
  </read_first>
  <behavior>
    - journal.rows type annotation is tuple[JournalRow, ...] after change
    - journal.rows.append(row) raises AttributeError (tuples have no append)
    - existing test_read_accepts_bytes still produces journal with len(journal.rows) == 3
    - _make_journal(*rows) in test_xlsx.py still works; all 19 xlsx tests pass
  </behavior>
  <action>
    In src/contaplus_reader/models.py: change the `rows` field on `ContaPlusJournal` from `rows: list[JournalRow]` to `rows: tuple[JournalRow, ...]`. Update the docstring to say "rows: immutable sequence of validated non-deleted non-memo journal lines." No other change to models.py.

    In src/contaplus_reader/_reader.py: at the return statement (line 211-215), change `rows=rows` to `rows=tuple(rows)`. The accumulator variable `rows` remains a plain `list[JournalRow]` throughout the function body — only the construction call changes.

    In tests/test_xlsx.py: change the `_make_journal` helper from `ContaPlusJournal(rows=list(rows), skipped_memo=0)` to `ContaPlusJournal(rows=rows, skipped_memo=0)` — the `*rows: JournalRow` variadic captures a tuple already, so this is correct. Alternatively keep `rows=tuple(rows)` for explicitness; either works since `tuple(tuple_arg)` is a no-op.

    In tests/test_reader.py: add a test `test_journal_rows_is_immutable_tuple` (place it after the ContaPlusJournal structure tests, near the existing field-shape tests). The test reads a valid DBF (use `cp850_basic_dbf` fixture), asserts `isinstance(journal.rows, tuple)`, and asserts `pytest.raises(AttributeError, match="append")` when calling `journal.rows.append`. No diario_dbf_builder needed.
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_xlsx.py tests/test_reader.py::test_journal_rows_is_immutable_tuple -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rows: tuple\[JournalRow" src/contaplus_reader/models.py` returns one match
    - `grep -n "rows=tuple(rows)" src/contaplus_reader/_reader.py` returns one match
    - test_journal_rows_is_immutable_tuple exits 0
    - All 19 xlsx tests pass (no list/tuple constructor breakage in _make_journal)
    - `uv run pytest tests/ -q` exits 0 with no failures (full suite green)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: CR-02 — Guard CLI read_bytes() against OSError and add test</name>
  <files>src/contaplus_reader/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/contaplus_reader/cli.py — read the full main() function; locate lines 51-65 (the existing try/except ContaPlusReadError block); understand the console setup (line 42: Console(stderr=True)) and the Panel import (line 19) — mirror the same pattern for the OSError handler
    - tests/test_cli.py — read in full to understand the CliRunner usage pattern (runner.invoke(app, [...])) and how result.output and result.exit_code are asserted
  </read_first>
  <behavior>
    - test_cli_nonexistent_input_exits_nonzero: invoke CLI with a path to a non-existent file; assert result.exit_code == 1; assert "Traceback" not in combined output (stdout + stderr)
    - existing test_cli_happy_path: still exits 0
    - existing test_cli_invalid_dbf_shows_error_panel: still exits 1 with panel (the ContaPlusReadError handler is unchanged)
  </behavior>
  <action>
    In src/contaplus_reader/cli.py: split the current single try block at lines 52-65 into two sequential try blocks. First block (new): wrap `raw_bytes = input_file.read_bytes()` in `try/except OSError as exc`. The handler prints a Rich Panel to console with title "File Read Error", border_style="red", and the str(exc) as body — then raises `typer.Exit(1) from None`. Second block (existing, unchanged): wrap `data = read(raw_bytes, source_name=str(input_file))` in the existing `except ContaPlusReadError` handler. The rest of the function (xlsx render, write, echo) is unchanged. The variable `raw_bytes` is introduced between the two blocks; the existing `data = read(...)` call uses `raw_bytes` instead of `input_file.read_bytes()`.

    In tests/test_cli.py: add test `test_cli_nonexistent_input_exits_nonzero` after the existing overwrite-guard tests. Use `runner.invoke(app, [str(tmp_path / "no_such_file.dbf"), str(tmp_path / "out.xlsx")])`. Assert `result.exit_code == 1`. Combine stdout and stderr for the traceback check: `combined = result.output + (result.stderr or "")` and assert `"Traceback" not in combined`. Also assert `"Traceback" not in (result.exception.__class__.__name__ if result.exception else "")` — or equivalently assert `result.exception is None` (since typer.Exit is not a real exception, CliRunner captures it as exit_code).
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_cli.py::test_cli_nonexistent_input_exits_nonzero tests/test_cli.py::test_cli_happy_path -v</automated>
  </verify>
  <acceptance_criteria>
    - cli.py contains `except OSError as exc` before the existing `except ContaPlusReadError` block
    - cli.py contains `raw_bytes = input_file.read_bytes()` (the read_bytes call is now in the OSError-guarded block)
    - test_cli_nonexistent_input_exits_nonzero exits 0 (the test itself passes)
    - test_cli_nonexistent_input_exits_nonzero asserts result.exit_code == 1 and "Traceback" not in output
    - test_cli_happy_path still exits 0
    - `uv run pytest tests/test_cli.py -v` shows all CLI tests passing
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: WR-02 — Fix numeric-zero column width undercount in xlsx.py</name>
  <files>src/contaplus_reader/xlsx.py, tests/test_xlsx.py</files>
  <read_first>
    - src/contaplus_reader/xlsx.py — read the column autofit section; locate line 74 (`max_len = max((len(str(c.value or "")) for c in col_cells), default=0)`)
  </read_first>
  <action>
    In src/contaplus_reader/xlsx.py at line 74: replace `len(str(c.value or ""))` with `len(str("" if c.value is None else c.value))`. This makes 0.0 contribute `len("0.0") = 3` to the column width instead of 0. No other changes. This is a WARNING-level fix (not a blocker), but it is a one-line change with zero risk.

    In tests/test_xlsx.py: add a test `test_column_width_not_truncated_by_zero_values` after the existing `test_column_widths_are_set` test. Build a journal with one row where haber=0.0 and debe=1500.75. Load the workbook, get the column dimension width for the Haber column (column E, index 5 in openpyxl). Assert the width is greater than 0 (it should reflect the "Haber" header length at minimum, not zero).
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_xlsx.py::test_column_width_not_truncated_by_zero_values -v</automated>
  </verify>
  <acceptance_criteria>
    - xlsx.py line 74 uses `c.value is None` not `c.value or ""`
    - test_column_width_not_truncated_by_zero_values exits 0
    - `uv run pytest tests/ -q` exits 0 (full suite green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied BinaryIO -> sniff() | Untrusted stream may be non-seekable; CR-01 fix closes raw OSError leak |
| user filesystem path -> CLI -> read_bytes() | Non-existent or unreadable path raises OSError; CR-02 fix wraps this boundary |
| parsed DBF records -> journal accumulation | Records with mixed-sign both-non-zero amounts bypass CR-04 guard (now fixed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ContaPlusJournal.rows (list → tuple) | mitigate | CR-03 fix: change to tuple[JournalRow, ...]; consumers cannot silently append/clear rows after construction |
| T-04-02 | Denial of Service | sniff() on non-seekable stream | mitigate | CR-01 fix: data.seekable() check before seek(0); OSError caught and re-raised as ContaPlusReadError rather than propagating unhandled |
| T-04-03 | Tampering | D-C2 guard accepting mixed-sign rows | mitigate | CR-04 fix: != 0 check; rows with both debe and haber populated (any sign combination) are rejected before reaching downstream consumers |
| T-04-04 | Information Disclosure | CLI leaking Python traceback to terminal | mitigate | CR-02 fix: OSError wrapped in Rich error panel; no stack frames exposed to end user |
| T-04-SC | Tampering | No new package manager installs in this plan | accept | Gap-closure plan modifies existing files only; no new npm/pip/cargo installs; Package Legitimacy Gate does not apply |
</threat_model>

<verification>
Run the full suite after all tasks complete:

```
cd C:/dev/contaplus-reader && uv run pytest -v
```

Expected: all tests pass (50 existing + 5 new = 55 tests minimum). Zero failures.

Spot-check the four VERIFICATION.md truths directly:

1. CR-04: `uv run pytest tests/test_reader.py::test_negative_debe_positive_haber_raises -v` exits 0
2. CR-02: `uv run pytest tests/test_cli.py::test_cli_nonexistent_input_exits_nonzero -v` exits 0
3. CR-03: `uv run pytest tests/test_reader.py::test_journal_rows_is_immutable_tuple -v` exits 0
4. CR-01: `uv run pytest tests/test_reader.py::test_non_seekable_binary_io_raises_structured_error -v` exits 0

Source assertions (grep gates):
- `grep -n "if debe != 0 and haber != 0" src/contaplus_reader/_reader.py` — one match
- `grep -n "rows: tuple\[JournalRow" src/contaplus_reader/models.py` — one match
- `grep -n "rows=tuple(rows)" src/contaplus_reader/_reader.py` — one match
- `grep -n "except OSError" src/contaplus_reader/cli.py` — one match
- `grep -v "^#" src/contaplus_reader/_sniffer.py | grep -c "data.seekable()"` — count >= 1
</verification>

<success_criteria>
1. `uv run pytest -v` exits 0 with 55+ tests (50 pre-existing + 5 new gap-closure tests)
2. All four VERIFICATION.md truths marked FAILED are now passing:
   - Truth 8 (CR-04 both-non-zero): test_negative_debe_positive_haber_raises passes
   - Truth 11 (CR-03 rows immutability): test_journal_rows_is_immutable_tuple passes; journal.rows is tuple
   - Truth 13 (CR-02 CLI error panel): test_cli_nonexistent_input_exits_nonzero passes
   - Non-seekable truth (CR-01): test_non_seekable_binary_io_raises_structured_error passes
3. Requirements INPUT-03, JRNL-03, API-02, CLI-01 upgraded from PARTIAL to SATISFIED
4. No regressions in the 50 existing tests
</success_criteria>

<output>
Create `.planning/phases/01-journal-slice/01-04-SUMMARY.md` when done.
</output>
