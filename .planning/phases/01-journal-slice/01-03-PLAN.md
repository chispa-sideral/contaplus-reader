---
phase: "01-journal-slice"
plan: 3
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - .gitignore
autonomous: false
requirements:
  - CLI-04
  - DIST-02

must_haves:
  truths:
    - "`uv build` completes without error and produces a .whl file in dist/"
    - "The wheel is named contaplus_reader-0.1.0-py3-none-any.whl (pure Python)"
    - "`uvx --from dist/contaplus_reader-0.1.0-py3-none-any.whl contaplus2xlsx --help` exits 0 and shows help text"
    - "The repo has a minimal README.md"
    - "pii-test-data/ and dist/ are gitignored"
  artifacts:
    - path: "dist/contaplus_reader-0.1.0-py3-none-any.whl"
      provides: "Installable wheel for PyPI + uvx consumption"
    - path: "README.md"
      provides: "Minimal project description for PyPI page"
      min_lines: 10
    - path: ".gitignore"
      provides: "Ignores dist/, pii-test-data/, __pycache__, .venv, *.egg-info"
      contains: "pii-test-data"
  key_links:
    - from: "dist/*.whl"
      to: "uvx contaplus2xlsx"
      via: "uvx --from dist/*.whl contaplus2xlsx"
      pattern: "contaplus2xlsx"
---

<objective>
Verify the wheel build succeeds and `uvx contaplus2xlsx` can install and run from
the locally-built wheel. This closes the walking skeleton: the CLI is now installable
and runnable without a checkout.

Purpose: DIST-02 + CLI-04 — the wheel is the artifact micropip will consume in Phase 5
and what PyPI will publish in Phase 4. Getting it green now proves the packaging is
correct before feature work accumulates.

Output:
- `dist/contaplus_reader-0.1.0-py3-none-any.whl` — verified buildable
- `README.md` — minimal project description (required for PyPI page; pyproject.toml references it)
- `.gitignore` — keeps dist/, pii-test-data/, and Python artifacts out of git
</objective>

<execution_context>
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/workflows/execute-plan.md
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-journal-slice/01-01-SUMMARY.md
@.planning/phases/01-journal-slice/01-02-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README + .gitignore + uv build</name>
  <files>
    README.md,
    .gitignore
  </files>
  <read_first>
    - pyproject.toml (verify readme = "README.md" is set — pyproject.toml requires README.md to exist at build time)
    - CLAUDE.md (license LGPL-3.0-or-later, project description, i18n: English everything)
  </read_first>
  <action>
    Create README.md with the following content sections (English, no emojis):
      - H1: `contaplus-reader`
      - One-line description: "Read Sage ContaPlus accounting exports (DIARIO.DBF, .zip) into usable formats."
      - ## Installation section: `uvx contaplus2xlsx` (recommended) and `pip install contaplus-reader` (library)
      - ## Usage section: `contaplus2xlsx DIARIO.DBF out.xlsx` with `--force` flag mentioned
      - ## License section: LGPL-3.0-or-later

    Create .gitignore:
      Include: dist/, __pycache__/, *.pyc, .venv/, *.egg-info/, .pytest_cache/,
               pii-test-data/, *.whl (local build artifacts), .env, *.tmp

    Then run: uv build

    This produces dist/contaplus_reader-0.1.0-py3-none-any.whl and
    dist/contaplus_reader-0.1.0.tar.gz. The wheel name must be
    contaplus_reader-0.1.0-py3-none-any.whl (pure-Python, no ABI tag).
  </action>
  <verify>
    <automated>uv build && python -c "import pathlib; wheels=list(pathlib.Path('dist').glob('*.whl')); assert wheels, 'No wheel produced'; print(f'Wheel: {wheels[0].name}')"</automated>
  </verify>
  <done>
    - README.md exists with Installation and Usage sections
    - .gitignore exists with pii-test-data/ and dist/ entries
    - `uv build` exits 0
    - `dist/contaplus_reader-0.1.0-py3-none-any.whl` exists
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 built: Python package scaffold, journal reader with D-A1..D-E3 rules,
    bytes→path bridge, magic-byte sniffer, all models, synthetic fixture factory,
    and ~27 reader tests.
    Plan 02 built: XLSX renderer with Spanish headers + accounting format, Typer CLI
    with --force guard and Rich error panel, XLSX tests, CLI tests.
    Plan 03 built: README.md, .gitignore, uv build wheel.
    Walking skeleton is now complete.
  </what-built>
  <how-to-verify>
    Step 1 — Run the test suite:
      uv run pytest tests/ -v
    Expected: all tests pass (green). Note the count for the summary.

    Step 2 — Verify the wheel builds and uvx runs:
      uv build
      uvx --from dist/contaplus_reader-0.1.0-py3-none-any.whl contaplus2xlsx --help
    Expected: help text appears showing <input_file> <output_file> [OPTIONS] and --force/--overwrite.

    Step 3 — End-to-end CLI test with a real synthetic DBF:
      uv run python -c "
      import tempfile, pathlib, datetime
      import dbf
      spec = 'ASIEN N(6,0); FECHA D; SUBCTA C(12); CONTRA C(12); CONCEPTO C(25); EURODEBE N(16,2); EUROHABER N(16,2)'
      with tempfile.NamedTemporaryFile(suffix='.dbf', delete=False) as f:
          p = pathlib.Path(f.name)
      t = dbf.Table(str(p), spec, codepage='cp850')
      t.open(mode=dbf.READ_WRITE)
      t.append({'FECHA': datetime.date(2025,1,15), 'SUBCTA': '4300001', 'EURODEBE': 150.0, 'EUROHABER': 0.0, 'CONCEPTO': 'Facturas'})
      t.close()
      print(p)
      "
      # Replace {path} with the printed path:
      uv run contaplus2xlsx {path} /tmp/test_out.xlsx
    Expected: Output line like "/tmp/test_out.xlsx — 1 journal rows"
    Then open /tmp/test_out.xlsx in Excel/LibreOffice to verify the journal sheet,
    Spanish headers, bold header row, and date/number formatting.

    Step 4 — Verify the overwrite guard:
      uv run contaplus2xlsx {path} /tmp/test_out.xlsx
    Expected: Exit code 1, "already exists" message shown.
      uv run contaplus2xlsx {path} /tmp/test_out.xlsx --force
    Expected: Exit code 0, row count summary printed.

    Step 5 — Verify error case:
      echo "not a dbf" > /tmp/bad.dbf
      uv run contaplus2xlsx /tmp/bad.dbf /tmp/bad_out.xlsx
    Expected: Exit code 1, Rich error panel with "ContaPlus Read Error" title, no Python traceback.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass as expected.
    If any step fails, describe what went wrong and which step failed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wheel → uvx install | uv resolves and installs the local wheel in an isolated environment |
| dist/*.whl | Build artifact; not committed to git (gitignored); PyPI publish is Phase 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `dist/*.whl` — wheel contains unexpected files or code | accept | `uv build` uses `uv_build` backend which only includes files under `src/`; no custom build hooks; pyproject.toml is the only config |
| T-03-02 | Information Disclosure | `pii-test-data/` accidentally committed via `git add .` | mitigate | `.gitignore` includes `pii-test-data/` entry; verify with `git status` before any commit |
| T-03-SC | Tampering | No new package installs in this plan | accept | No `uv add` commands; no new packages; T-01-SC mitigation from Plan 01 covers existing deps |
</threat_model>

<verification>
1. `uv run pytest tests/ -v` — all tests pass
2. `uv build` exits 0; `dist/contaplus_reader-0.1.0-py3-none-any.whl` exists
3. `uvx --from dist/contaplus_reader-0.1.0-py3-none-any.whl contaplus2xlsx --help` exits 0
4. Human verification confirms E2E CLI, overwrite guard, and error panel behavior
5. `git -v status` shows `pii-test-data/` is untracked (gitignored)
</verification>

<success_criteria>
- `uv run pytest tests/ -v` all green
- `uv build` produces `dist/contaplus_reader-0.1.0-py3-none-any.whl`
- `uvx --from dist/*.whl contaplus2xlsx --help` exits 0
- Human confirms the CLI end-to-end smoke test passes (Step 3 of checkpoint)
- `pii-test-data/` is gitignored

Phase 1 success criteria (from ROADMAP.md) met:
  1. `contaplus2xlsx DIARIO.DBF out.xlsx` produces a styled .xlsx with validated journal rows
  2. CLI rejects invalid .dbf with Rich error panel naming offending row and column
  3. `uv build` produces a wheel; `uvx contaplus2xlsx` installs and runs from it
  4. Test suite uses synthetic blob-free fixtures; all ported tw-contaplus journal coverage passes
  5. `read(data: bytes | BinaryIO)` is the only entry point; no filesystem-path argument
</success_criteria>

<output>
Create `.planning/phases/01-journal-slice/01-03-SUMMARY.md` when done.
</output>
