---
phase: 02-zip-subaccounts
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_reader.py
  - tests/test_cli.py
  - tests/test_xlsx.py
autonomous: true
requirements:
  - INPUT-02
  - INPUT-04
  - INPUT-05
  - INPUT-06
  - TABL-01
  - TABL-02
  - API-05
  - CLI-02

must_haves:
  truths:
    - "Every Phase 2 behavior has a failing automated test before any implementation exists"
    - "Synthetic ZIP, SUBCTA, and group-table DBF fixtures are generated at collection time with no binary blobs committed"
    - "All 55 Phase 1 tests still pass alongside the new failing tests"
    - "Test cases exist covering all 4 Phase 2 ROADMAP success criteria (zip->xlsx enriched, multi-company error, zip-slip rejection, multi-sheet output), confirmed RED before 02-02/02-03 implementation lands"
  artifacts:
    - path: "tests/conftest.py"
      provides: "ZIP + SUBCTA + group-table fixture builders"
      contains: "single_company_zip, multi_company_zip, subcta_dbf_builder, single_company_zip_with_subcta"
    - path: "tests/test_reader.py"
      provides: "Failing tests for ZIP reading, zip-slip, company resolution, SUBCTA enrichment"
      min_lines: 150
    - path: "tests/test_cli.py"
      provides: "Failing tests for --company flag, multi-company error panel"
      min_lines: 40
    - path: "tests/test_xlsx.py"
      provides: "Failing tests for multi-sheet workbook and Descripcion column"
      min_lines: 40
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_reader.py"
      via: "session-scoped fixtures"
      pattern: "single_company_zip|multi_company_zip|subcta_dbf_builder"
    - from: "tests/conftest.py"
      to: "tests/test_xlsx.py"
      via: "session-scoped fixtures"
      pattern: "single_company_zip_with_subcta"
  decisions:
    - "D-17: schema-discovery gate — synthetic fixtures encode the SUBCTA/group-table field schemas enumerated in pii-test-data/ research; fixture specs (_SUBCTA_SPEC, _GRUPOS_SPEC, _USUARIOS_SPEC, _EMPRESA_SPEC) are derived from discovered schemas, not invented"
---

## Phase Goal

**As a** ContaPlus migrant, **I want to** convert a full ContaPlus backup `.zip` — including subaccount name enrichment — to a multi-sheet `.xlsx`, **so that** I have all my accounting data in one usable workbook without any install or upload.

<objective>
Extend the test suite with all synthetic fixtures and failing test cases for Phase 2 behaviors before any implementation exists. This plan is the Wave 0 Nyquist gate: no implementation task may proceed without the corresponding test already written and confirmed RED.

Purpose: Ensure all 19 Phase 2 behaviors have automated verification before implementation begins, following the TDD RED phase. The test infrastructure also prevents regressions on the 55 Phase 1 tests.

Output: Four extended test files with synthetic ZIP, SUBCTA, and group-table fixtures, plus failing test stubs for every requirement in this phase.
</objective>

<execution_context>
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/workflows/execute-plan.md
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-zip-subaccounts/02-CONTEXT.md
@.planning/phases/02-zip-subaccounts/02-RESEARCH.md
@.planning/phases/02-zip-subaccounts/02-PATTERNS.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/contaplus_reader/models.py:
  ContaPlusReadError(message, row_index=-1, column=None, original=None)
  JournalRow(fecha, cuenta, subcuenta, debe, haber, concepto)
  ContaPlusJournal(rows: tuple[JournalRow,...], skipped_memo=0, source_name=None)
  ContaPlusData(journal=None)

From src/contaplus_reader/__init__.py:
  read(data: bytes | BinaryIO, source_name: str | None = None) -> ContaPlusData

From tests/conftest.py (current session fixtures):
  cp850_basic_dbf(tmp_path_factory) -> Path  -- 3-row DIARIO.DBF
  diario_dbf_builder(tmp_path_factory) -> Callable[[list[dict]], Path]
  _build_diario_dbf(target, rows, *, codepage="cp850") -> Path  -- module-level helper
  _DIARIO_SPEC = "ASIEN N(6,0); FECHA D; SUBCTA C(12); CONTRA C(12); CONCEPTO C(25); EURODEBE N(16,2); EUROHABER N(16,2)"
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Extend conftest.py with ZIP and table fixture builders</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (full file — understand existing builders and import block)
    - .planning/phases/02-zip-subaccounts/02-PATTERNS.md (§conftest.py pattern — SUBCTA builder, single_company_zip, multi_company_zip, assumed group-table specs)
    - .planning/phases/02-zip-subaccounts/02-RESEARCH.md (§Critical Finding: Missing Group Tables — synthetic specs; §SUBCTA Schema — _SUBCTA_SPEC)
  </read_first>
  <behavior>
    - _build_subcta_dbf(target, rows, *, codepage="cp850") -> Path: creates SubCta.dbf using spec "cod C(12,0); titulo C(40,0); nif C(15,0)"
    - single_company_zip_with_subcta fixture: creates backup.zip with Emp01/Diario.dbf (from cp850_basic_dbf) and Emp01/SubCta.dbf containing rows [{"cod":"4300000","titulo":"Cliente XYZ","nif":""}, {"cod":"7000000","titulo":"Ventas mercaderias","nif":""}]
    - multi_company_zip fixture: creates multi.zip with Emp01/Diario.dbf and Emp02/Diario.dbf (both using cp850_basic_dbf bytes)
    - single_company_zip fixture (no SUBCTA): creates backup_nosub.zip with Emp01/Diario.dbf only (for enrichment_no_subcta tests)
    - _build_generic_dbf(target, spec, rows, *, codepage="cp850") -> Path: generic builder for assumed group-table schemas
    - Assumed group-table specs: _GRUPOS_SPEC="COD C(10,0); DESCRIP C(40,0)", _USUARIOS_SPEC="CODIGO C(10,0); NOMBRE C(40,0); CLAVE C(20,0)", _EMPRESA_SPEC="CODIGO C(10,0); NOMBRE C(60,0); NIF C(15,0)"
    - zip_with_group_tables fixture: creates backup_groups.zip with Emp01/Diario.dbf, Emp01/SubCta.dbf, Emp01/grupos.dbf, Emp01/usuarios.dbf, Emp01/empresa.dbf using assumed specs
    - zip_slip_zip fixture: creates a ZIP with an entry named "../evil.dbf" (path-traversal entry) for zip-slip tests
  </behavior>
  <action>
    Add `import zipfile` to the imports block (after existing imports).

    Add module-level constants _SUBCTA_SPEC, _GRUPOS_SPEC, _USUARIOS_SPEC, _EMPRESA_SPEC below _DIARIO_SPEC.

    Add _build_subcta_dbf and _build_generic_dbf module-level helpers following the exact _build_diario_dbf pattern (dbf.Table + open + append + close).

    Add session-scoped fixtures: single_company_zip_with_subcta, single_company_zip (no subcta), multi_company_zip, zip_with_group_tables, zip_slip_zip.

    For zip_slip_zip: use zipfile.ZipFile in write mode; add a BytesIO content as member with arcname="../evil.dbf" (use zf.writestr("../evil.dbf", b"") because zf.write requires a real path). This produces a ZIP with a path-traversal entry.

    All fixtures are scope="session". All use tmp_path_factory. No real ContaPlus data enters the repo.
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>conftest.py imports zipfile; all new fixture functions are present and importable; `uv run pytest --collect-only -q` collects without errors (a syntax error or broken fixture in conftest.py causes collection failure)</done>
</task>

<task type="tdd">
  <name>Task 2: Write failing tests for ZIP reader and SUBCTA enrichment</name>
  <files>tests/test_reader.py, tests/test_cli.py, tests/test_xlsx.py</files>
  <read_first>
    - tests/test_reader.py (full file — understand test structure, imports, parametric patterns)
    - tests/test_cli.py (full file — understand CLI test patterns, typer.testing.CliRunner usage)
    - tests/test_xlsx.py (full file — understand xlsx assertion patterns, openpyxl load_workbook)
    - .planning/phases/02-zip-subaccounts/02-RESEARCH.md (§Phase Requirements -> Test Map — exact test function names and -k filter values)
    - .planning/phases/02-zip-subaccounts/02-VALIDATION.md (Wave 0 gaps table)
  </read_first>
  <behavior>
    All tests in this task MUST fail (ImportError on missing modules OR pytest.xfail OR assertion failure) — they are RED tests to be made GREEN in Plan 02-02 and 02-03.

    tests/test_reader.py — add the following test functions (each as def test_<name>):
    - test_read_zip_bytes: read(zip_bytes) returns ContaPlusData with journal; uses single_company_zip_with_subcta fixture, reads file as bytes
    - test_read_zip_filelike: read(BinaryIO) works for ZIP; wrap same zip in io.BytesIO
    - test_read_zipslip_rejected: read(zip_slip_zip bytes) raises ContaPlusReadError with "Unsafe ZIP entry" in message
    - test_read_multi_company_no_selector: read(multi_company_zip bytes) raises ContaPlusReadError with "Emp01" and "Emp02" in message
    - test_read_multi_company_selected: read(multi_company_zip bytes, company="Emp01") returns ContaPlusData with journal.rows having >0 entries
    - test_read_single_company_wrong_selector: read(single_company_zip bytes, company="WrongCo") raises ContaPlusReadError
    - test_read_selector_on_dbf_raises: read(cp850_basic_dbf bytes, company="Emp01") raises ContaPlusReadError with "not applicable" in message
    - test_subcta_lookup_correct: read(single_company_zip_with_subcta bytes) -> rows where subcuenta=="4300000" have subcuenta_nombre=="Cliente XYZ"; rows where subcuenta=="7000000" have subcuenta_nombre=="Ventas mercaderias"
    - test_subcta_lookup_missing_key: read(single_company_zip_with_subcta bytes) -> rows for subcuenta not in SUBCTA have subcuenta_nombre is None
    - test_subcta_absent_all_none: read(single_company_zip bytes, no subcta) -> all rows have subcuenta_nombre is None
    - test_subcta_candidate_fallback: build a ZIP with a SubCta.dbf using "CODIGO C(12,0); DESCRIP C(40,0)" field names; read() must still resolve them via _pick_column candidate list and return rows with subcuenta_nombre populated
    - test_group_tables_absent_no_error: read(single_company_zip_with_subcta bytes) returns ContaPlusData where grupos is None, usuarios is None, empresa is None (no error raised)
    - test_usuarios_present_in_result: read(zip_with_group_tables bytes) returns ContaPlusData where usuarios is not None

    tests/test_cli.py — add:
    - test_cli_company_flag_accepted: invoke CLI with [str(zip_path), str(out), "--company", "Emp01"]; result.exit_code == 0 (or verify --company is a valid option)
    - test_cli_multi_company_no_flag_exits_1: invoke CLI with [str(multi_zip_path), str(out)] (no --company); result.exit_code == 1 and "Emp01" in result.output (or stderr)

    tests/test_xlsx.py — add:
    - test_render_multi_sheet_names: build a ContaPlusData with a mock journal and a mock subcta; call render(data); open with openpyxl; assert sheetnames contains "Diario" and "Subcuentas"
    - test_render_descripcion_column_present: build a ContaPlusData where journal rows have subcuenta_nombre="Cliente XYZ"; call render(data); open Diario sheet; assert column header "Descripción" is in row 1 and that row 2's Descripción cell value is "Cliente XYZ"

    All tests must be importable at collection time (no NameError). Tests that reference not-yet-existing imports (e.g., from contaplus_reader.models import SubctaTable) must use pytest.importorskip or a try/except import guard so collection succeeds but the test is marked as xfail or error at runtime.

    Preferred pattern for not-yet-existing symbols: import at function body level inside the test with pytest.raises(ImportError) OR use pytest.mark.xfail(strict=False, reason="Phase 2 implementation not yet present").
  </behavior>
  <action>
    In tests/test_reader.py: add import io at top (if not present). Add all test functions listed above. Each function references fixtures by name as parameters.

    For test_subcta_candidate_fallback: build the alternate-schema SubCta.dbf inline in the test using _build_generic_dbf (imported from conftest) with spec "CODIGO C(12,0); DESCRIP C(40,0)" and rows [{"CODIGO":"4300000","DESCRIP":"Cliente XYZ"}], then build a ZIP in a tmp_path directory and call read().

    In tests/test_cli.py: import CliRunner from typer.testing (already present pattern). Add test functions; use fixture zip paths as Path args converted to str. The --company flag test should verify the option is accepted; at this point the CLI does not support it, so the test will fail at collection or invocation — that is the desired RED state.

    In tests/test_xlsx.py: import openpyxl at top (already present pattern). For render multi-sheet tests, import render from contaplus_reader.xlsx at function body level so ImportError (render not defined yet) is caught as test failure, not collection failure. Use pytest.mark.xfail(strict=False) on all new tests in test_xlsx.py and test_cli.py that reference symbols not yet created.

    Do NOT use pytest.xfail() call inside test bodies (that marks as expected-pass-xfail). Use pytest.mark.xfail(strict=False) as a decorator so the test runs, fails, and is reported as xfail — it will automatically become xpass when the implementation lands.
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    `uv run pytest -q` output shows:
    - All 55 Phase 1 tests still PASS (no regressions)
    - New tests appear as FAILED or XFAIL (not ERROR — collection must succeed)
    - No ImportError at collection time
    - Total collected count > 70
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> filesystem | Synthetic DBF/ZIP written to tmp_path; no real ContaPlus data enters repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2; all deps already in pyproject.toml |
</threat_model>

<verification>
Run `uv run pytest -q` — all 55 Phase 1 tests pass; new tests are FAILED or XFAIL; no collection errors.
</verification>

<success_criteria>
- conftest.py exports: single_company_zip_with_subcta, single_company_zip, multi_company_zip, zip_with_group_tables, zip_slip_zip, _build_subcta_dbf, _build_generic_dbf
- tests/test_reader.py has 13 new test functions, all importable, all RED (fail or xfail)
- tests/test_cli.py has 2 new test functions, importable, RED
- tests/test_xlsx.py has 2 new test functions, importable, RED (xfail)
- `uv run pytest -q` exits non-zero (failures expected) but collects without error
- No binary blobs committed
</success_criteria>

<output>
Create `.planning/phases/02-zip-subaccounts/02-01-SUMMARY.md` when done.
</output>
