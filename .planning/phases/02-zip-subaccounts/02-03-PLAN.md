---
phase: 02-zip-subaccounts
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/contaplus_reader/xlsx.py
  - src/contaplus_reader/cli.py
autonomous: true
requirements:
  - INPUT-02
  - INPUT-06
  - TABL-01
  - TABL-02
  - API-05
  - CLI-02

must_haves:
  truths:
    - "contaplus2xlsx backup.zip out.xlsx produces a multi-sheet .xlsx with Diario, Subcuentas sheets and Descripción column populated"
    - "contaplus2xlsx backup.zip out.xlsx --company Emp01 selects the correct company in a multi-company ZIP"
    - "contaplus2xlsx multi.zip out.xlsx (without --company) exits 1 and displays a Rich error panel listing available company directories"
    - "The Diario sheet has a Descripción column immediately after the Subcuenta column, blank when subcuenta_nombre is None"
    - "Non-journal sheets (Subcuentas, Grupos, Usuarios, Empresa) appear only when the corresponding table is non-None in ContaPlusData"
    - "All 55 Phase 1 tests, all 13 reader tests, 2 CLI tests, and 2 xlsx tests from Plan 02-01 are GREEN"
  artifacts:
    - path: "src/contaplus_reader/xlsx.py"
      provides: "render(data: ContaPlusData) -> bytes; render_journal() backward-compat alias"
      exports: ["render", "render_journal"]
      contains: "Descripción"
    - path: "src/contaplus_reader/cli.py"
      provides: "contaplus2xlsx with --company option; multi-sheet XLSX output"
      contains: "--company"
  key_links:
    - from: "src/contaplus_reader/cli.py"
      to: "src/contaplus_reader/xlsx.py"
      via: "render(data) replaces render_journal(journal)"
      pattern: "from contaplus_reader.xlsx import render"
    - from: "src/contaplus_reader/cli.py"
      to: "src/contaplus_reader/__init__.py"
      via: "read(raw_bytes, source_name=..., company=company)"
      pattern: "company=company"
    - from: "src/contaplus_reader/xlsx.py"
      to: "src/contaplus_reader/models.py"
      via: "ContaPlusData.subcta / .grupos / .usuarios / .empresa"
      pattern: "data\\.subcta|data\\.grupos|data\\.usuarios|data\\.empresa"
  decisions:
    - "D-07: --company flag pulled into Phase 2; CLI accepts --company Emp01 and passes it to read()"
    - "D-08: multi-company ZIP without --company surfaces as Rich error panel listing company directory names; CLI exits 1"
    - "D-12: renderer is data-driven -- one sheet per populated table on ContaPlusData; Phase-1 D-10/D-12 styling applied consistently"
    - "D-13: full dump -- each sheet reproduces every field its table contains in DBF field order; non-journal headers are raw DBF field names"
    - "D-14: sheet tabs use Spanish names: Diario, Subcuentas, Empresa, Grupos, Usuarios; code/CLI/errors stay English"
    - "D-15: Diario sheet gains Descripcion column after Subcuenta column, populated from subcuenta_nombre; blank when None"
---

## Phase Goal

**As a** ContaPlus migrant, **I want to** convert a full ContaPlus backup `.zip` — including subaccount name enrichment — to a multi-sheet `.xlsx`, **so that** I have all my accounting data in one usable workbook without any install or upload.

<objective>
Deliver the multi-sheet XLSX renderer and the `--company` CLI flag: the final two delivery layers that make the end-to-end user story true. After this plan, `contaplus2xlsx backup.zip out.xlsx` produces a styled workbook with a Diario sheet (including Descripción column) plus sheets for every secondary table present.

Purpose: Complete Phase 2 by wiring the rendering and CLI layers over the ZIP reader built in Plan 02-02.

Output: xlsx.py generalized to render(ContaPlusData); cli.py extended with --company; 4 new tests GREEN; Phase 2 success criteria all satisfied.
</objective>

<execution_context>
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/workflows/execute-plan.md
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-zip-subaccounts/02-CONTEXT.md
@.planning/phases/02-zip-subaccounts/02-PATTERNS.md
@.planning/phases/02-zip-subaccounts/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase after Plan 02-02 lands. -->

From src/contaplus_reader/models.py (Phase 2 state):
  JournalRow(fecha, cuenta, subcuenta, debe, haber, concepto, subcuenta_nombre: str | None = None)
  ContaPlusData(journal=None, subcta=None, empresa=None, grupos=None, usuarios=None)
  SubctaTable(rows: tuple[SubctaRow,...], source_name: str | None = None)
  GenericTable(headers: tuple[str,...], rows: tuple[tuple[object,...],...], source_name: str | None = None)

From src/contaplus_reader/__init__.py (Phase 2 state):
  read(data: bytes | BinaryIO, source_name: str | None = None, company: str | None = None) -> ContaPlusData

From src/contaplus_reader/xlsx.py (current Phase 1 state):
  render_journal(journal: ContaPlusJournal) -> bytes
  HEADERS = ["Fecha", "Cuenta", "Subcuenta", "Debe", "Haber", "Concepto"]
  ACCOUNTING_FMT, DATE_FMT, HEADER_FILL, HEADER_FONT  -- module-level styling constants

From src/contaplus_reader/cli.py (current Phase 1 state):
  main(input_file, output_file, force=False)  -- reads, renders with render_journal, writes
  Console(stderr=True) pattern; typer.Exit(1) from None pattern; Rich Panel error pattern
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize xlsx.py to render(ContaPlusData) with multi-sheet and Descripción column</name>
  <files>src/contaplus_reader/xlsx.py</files>
  <read_first>
    - src/contaplus_reader/xlsx.py (full file — understand render_journal, HEADERS, styling constants)
    - .planning/phases/02-zip-subaccounts/02-PATTERNS.md (§xlsx.py patterns — data-driven renderer, Diario sheet with Descripción, generic sheet renderer, render_journal backward-compat alias)
    - .planning/phases/02-zip-subaccounts/02-CONTEXT.md (D-12: data-driven one-sheet-per-table; D-13: full dump, raw DBF field names as headers; D-14: Spanish tab names Diario/Subcuentas/Empresa/Grupos/Usuarios; D-15: Descripción column after Subcuenta)
  </read_first>
  <behavior>
    - HEADERS changes from ["Fecha","Cuenta","Subcuenta","Debe","Haber","Concepto"] to ["Fecha","Cuenta","Subcuenta","Descripción","Debe","Haber","Concepto"] (D-15: Descripción inserted after Subcuenta at index 3)
    - New function render(data: ContaPlusData) -> bytes: creates Workbook; calls _render_journal_sheet(wb.active, data.journal) for sheet 1 (titled "Diario" — wb.active.title = "Diario"); if data.subcta is not None: _render_subcta_sheet(wb.create_sheet("Subcuentas"), data.subcta); if data.empresa is not None: _render_generic_sheet(wb.create_sheet("Empresa"), data.empresa); if data.grupos is not None: _render_generic_sheet(wb.create_sheet("Grupos"), data.grupos); if data.usuarios is not None: _render_generic_sheet(wb.create_sheet("Usuarios"), data.usuarios); save to BytesIO and return bytes
    - _render_journal_sheet(ws, journal: ContaPlusJournal) -> None: private; writes HEADERS to row 1 with HEADER_FILL/HEADER_FONT; freeze_panes = "A2"; for each jr in journal.rows: col 1=fecha (DATE_FMT), col 2=cuenta, col 3=subcuenta, col 4=jr.subcuenta_nombre (blank if None), col 5=debe (ACCOUNTING_FMT), col 6=haber (ACCOUNTING_FMT), col 7=concepto; auto-size all columns (same pattern as Phase 1)
    - _render_subcta_sheet(ws, subcta: SubctaTable) -> None: private; extract headers from first row's fields dict keys (in insertion order); apply HEADER_FILL/HEADER_FONT; freeze_panes = "A2"; write all field values per row; auto-size
    - _render_generic_sheet(ws, table: GenericTable) -> None: private; table.headers are raw DBF field names (D-13); apply HEADER_FILL/HEADER_FONT; freeze_panes = "A2"; for each row in table.rows: write values; auto-size
    - render_journal(journal: ContaPlusJournal) -> bytes: backward-compat alias; from contaplus_reader.models import ContaPlusData (inside function to avoid circular import risk); return render(ContaPlusData(journal=journal))
    - Update imports: add from contaplus_reader.models import ContaPlusData, SubctaTable, GenericTable, ContaPlusJournal
  </behavior>
  <action>
    Update HEADERS constant to insert "Descripción" at index 3 (after "Subcuenta", before "Debe"). Spanish character é is required — do not use an ASCII approximation.

    Extract the existing render_journal body into a private _render_journal_sheet(ws, journal) function. Update the column numbering: col 4 is now Descripción (value=jr.subcuenta_nombre, no format); col 5=debe, col 6=haber, col 7=concepto.

    Add render(data: ContaPlusData) -> bytes as the new primary entry point per the PATTERNS.md §xlsx.py pattern. Sheet order per D-14: Diario first (wb.active), then Subcuentas, Empresa, Grupos, Usuarios (only if non-None).

    Add _render_subcta_sheet and _render_generic_sheet private functions.

    Keep render_journal as a backward-compatible alias calling render(ContaPlusData(journal=journal)).

    Update module-level imports to include ContaPlusData, SubctaTable, GenericTable from contaplus_reader.models.
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_xlsx.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - render() is importable from contaplus_reader.xlsx
    - All Phase 1 XLSX tests still pass (render_journal backward compat confirmed)
    - New multi-sheet test (test_render_multi_sheet_names) is GREEN
    - New Descripción test (test_render_descripcion_column_present) is GREEN
    - Sheet named "Diario" has 7 columns; column 4 header is "Descripción"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --company flag to CLI and wire render(data) for multi-sheet output</name>
  <files>src/contaplus_reader/cli.py</files>
  <read_first>
    - src/contaplus_reader/cli.py (full file — understand main() signature, render_journal call, success summary block)
    - .planning/phases/02-zip-subaccounts/02-PATTERNS.md (§cli.py patterns — extended main() signature, updated read() call, render(data) call, multi-sheet success summary)
    - .planning/phases/02-zip-subaccounts/02-CONTEXT.md (D-07: --company flag; D-08: Rich error panel for missing --company; note CLI docstring says "Path to DIARIO.DBF or backup .zip")
  </read_first>
  <behavior>
    - main() signature gains company: Annotated[str | None, typer.Option("--company", help="Company directory name (e.g. Emp01) for multi-company ZIP backups")] = None inserted between output_file and force parameters
    - CLI docstring updated: "Convert a ContaPlus DIARIO.DBF or backup .zip to a styled .xlsx workbook"
    - input_file help text updated: "Path to DIARIO.DBF file or backup .zip"
    - read() call changes to: data = read(raw_bytes, source_name=str(input_file), company=company)
    - xlsx render call changes from: xlsx_bytes = render_journal(journal) to: from contaplus_reader.xlsx import render; xlsx_bytes = render(data)
    - Remove the local `journal = data.journal` line (no longer needed for xlsx rendering)
    - Keep `journal = data.journal` only for the success summary that references journal.rows and journal.skipped_memo
    - Success summary changes to multi-sheet format: count non-None tables in [data.journal, data.subcta, data.empresa, data.grupos, data.usuarios]; emit f"{output_file} — {row_count} journal rows, {sheet_count} sheet(s){skip_msg}" per PATTERNS.md §cli.py updated success summary block
    - The existing Rich error panel (except ContaPlusReadError block) handles the multi-company ContaPlusReadError message transparently via exc.message — no change needed there (D-08)
    - Remove the render_journal import from the imports inside main()
  </behavior>
  <action>
    Add company parameter to main() after output_file, before force. Use Annotated[str | None, typer.Option("--company", help="...")] = None syntax.

    Update the read() call to pass company=company.

    Replace `xlsx_bytes = render_journal(journal)` with `from contaplus_reader.xlsx import render; xlsx_bytes = render(data)`.

    Update the success summary to count sheets and use the multi-line f-string from PATTERNS.md. The count expression is: `sum(1 for t in [data.journal, data.subcta, data.empresa, data.grupos, data.usuarios] if t is not None)`.

    Update the CLI input_file help and docstring text to mention ".zip".

    Do not change the --force flag, the output_file existence check, the OSError handler, or the ContaPlusReadError handler — they remain unchanged.
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_cli.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - All Phase 1 CLI tests pass (backward compat — --company is optional, defaults to None)
    - test_cli_company_flag_accepted is GREEN
    - test_cli_multi_company_no_flag_exits_1 is GREEN
    - `uv run pytest -q` shows 0 failures across all 4 test files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 02-03 complete: multi-sheet XLSX renderer + --company CLI flag. The full Phase 2 feature is now implemented:
    - contaplus2xlsx backup.zip out.xlsx — produces multi-sheet .xlsx with Diario (Descripción column), Subcuentas, Grupos, Usuarios, Empresa sheets as present
    - contaplus2xlsx backup.zip out.xlsx --company Emp01 — selects company in multi-company ZIP
    - All 4 phase success criteria should hold against synthetic fixtures
  </what-built>
  <how-to-verify>
    1. Run the full test suite: `cd C:/dev/contaplus-reader && uv run pytest -q`
       Expected: 0 failures, all tests pass (including 17+ new Phase 2 tests)

    2. Run a quick end-to-end smoke test using a real ContaPlus ZIP from pii-test-data (if available):
       ```
       cd C:/dev/contaplus-reader
       uv run contaplus2xlsx pii-test-data/<any-single-company>.zip /tmp/out.xlsx
       ```
       Expected: success message like "out.xlsx — N journal rows, 2 sheet(s)"

    3. Open the resulting /tmp/out.xlsx and verify:
       - "Diario" tab exists with headers: Fecha, Cuenta, Subcuenta, Descripción, Debe, Haber, Concepto
       - "Subcuentas" tab exists with all SUBCTA fields
       - Descripción column has subaccount names (not blank) for recognized subcuenta codes

    4. Test multi-company error (if pii-test-data has a multi-company ZIP):
       ```
       uv run contaplus2xlsx pii-test-data/multi-company.zip /tmp/out.xlsx
       ```
       Expected: exit 1 with Rich error panel listing the company directory names

    Note: pii-test-data is confidential — do NOT quote file contents, company names, or account data in output or commits.
  </how-to-verify>
  <resume-signal>Type "approved" if tests pass and the XLSX looks correct. Describe any issues otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI --company flag → read() | User-supplied company name; case-insensitive match inside _zip.py; invalid selector raises ContaPlusReadError handled by existing Rich panel |
| ContaPlusData → openpyxl Workbook → bytes | Extracted field values written as cell values; no formula injection possible — openpyxl writes literal values only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | xlsx.py _render_generic_sheet | accept | DBF field values written as openpyxl cell literals; openpyxl does not interpret =FORMULA strings as formulas when set via cell.value — no XLSX formula injection risk |
| T-02-05 | Information Disclosure | usuarios.dbf credential fields in XLSX | accept | D-18 explicitly accepted: fully local conversion (CLI on user's machine, PWA is client-side); user is converting their own data; no network egress |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 |
</threat_model>

<verification>
Full suite: `uv run pytest -q` — 0 failures.

Phase 2 success criteria (automated):
1. `uv run pytest tests/test_cli.py -k zip_to_xlsx_enriched -v` — GREEN (Success Criterion #1)
2. `uv run pytest tests/test_cli.py -k multi_company_error -v` — GREEN (Success Criterion #2)
3. `uv run pytest tests/test_reader.py -k zipslip -v` — GREEN (Success Criterion #3)
4. `uv run pytest tests/test_xlsx.py -k multi_sheet -v` — GREEN (Success Criterion #4)
</verification>

<success_criteria>
- `render(ContaPlusData)` produces a multi-sheet .xlsx; sheet order: Diario, Subcuentas, Empresa, Grupos, Usuarios
- Diario sheet column 4 header is "Descripción"; value is subcuenta_nombre or blank
- render_journal(journal) backward-compat alias still works (Phase 1 xlsx tests pass)
- CLI --company option accepted and passed to read()
- Multi-company ZIP without --company exits 1 with Rich panel (handled by existing ContaPlusReadError block)
- `uv run pytest -q` — 0 failures across all test files
- No binary blobs committed; all fixtures synthetic
</success_criteria>

<output>
Create `.planning/phases/02-zip-subaccounts/02-03-SUMMARY.md` when done.
</output>
