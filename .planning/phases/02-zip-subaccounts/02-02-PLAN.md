---
phase: 02-zip-subaccounts
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/contaplus_reader/_sniffer.py
  - src/contaplus_reader/_zip.py
  - src/contaplus_reader/_subcta.py
  - src/contaplus_reader/_reader.py
  - src/contaplus_reader/models.py
  - src/contaplus_reader/__init__.py
autonomous: true
requirements:
  - INPUT-02
  - INPUT-04
  - INPUT-05
  - INPUT-06
  - TABL-01
  - TABL-02
  - API-05

must_haves:
  truths:
    - "read(zip_bytes) returns ContaPlusData with journal rows that have subcuenta_nombre populated from SUBCTA.DBF"
    - "A path-traversal ZIP entry raises ContaPlusReadError with 'Unsafe ZIP entry' in the message before extraction"
    - "A multi-company ZIP without a company selector raises ContaPlusReadError listing available company directory names"
    - "A raw DBF with a company selector raises ContaPlusReadError with 'not applicable' in the message"
    - "SUBCTA absent from ZIP produces all subcuenta_nombre=None rows — no error"
    - "Group tables (grupos/usuarios/empresa) absent from ZIP produce None attributes — no error"
    - "Group tables present in ZIP are read and accessible as non-None attributes on ContaPlusData"
    - "All 55 Phase 1 tests plus the 17 new reader/SUBCTA tests from Plan 02-01 are GREEN"
  artifacts:
    - path: "src/contaplus_reader/_zip.py"
      provides: "_safe_extract_zip, _resolve_company_diario, _find_sibling_dbf"
      exports: ["_safe_extract_zip", "_resolve_company_diario", "_find_sibling_dbf"]
    - path: "src/contaplus_reader/_subcta.py"
      provides: "build_subcta_lookup, read_subcta_table, read_table_raw"
      exports: ["build_subcta_lookup", "read_subcta_table", "read_table_raw"]
    - path: "src/contaplus_reader/models.py"
      provides: "JournalRow with subcuenta_nombre; ContaPlusData with subcta/empresa/grupos/usuarios; SubctaTable, GenericTable, SubctaRow"
      contains: "subcuenta_nombre"
    - path: "src/contaplus_reader/_sniffer.py"
      provides: "sniff() returns 'dbf' or 'zip' string literal"
      contains: "return"
    - path: "src/contaplus_reader/__init__.py"
      provides: "read() dispatches on fmt; ZIP path uses TemporaryDirectory; company param"
      contains: "company"
  key_links:
    - from: "src/contaplus_reader/__init__.py"
      to: "src/contaplus_reader/_zip.py"
      via: "_safe_extract_zip, _resolve_company_diario"
      pattern: "from contaplus_reader._zip import"
    - from: "src/contaplus_reader/__init__.py"
      to: "src/contaplus_reader/_subcta.py"
      via: "build_subcta_lookup, read_subcta_table, read_table_raw"
      pattern: "from contaplus_reader._subcta import build_subcta_lookup, read_subcta_table, read_table_raw"
    - from: "src/contaplus_reader/_reader.py"
      to: "src/contaplus_reader/models.py"
      via: "subcuenta_nombre field on JournalRow"
      pattern: "subcuenta_nombre"
  decisions:
    - "D-01: ZIP detected by magic-byte sniff — _sniffer.py PK-branch flipped from rejection to acceptance, returning 'zip'"
    - "D-02: multi-company disambiguation by raw company directory name (case-insensitive); error lists available names"
    - "D-03: company-selector edge behavior — wrong selector on single-company ZIP raises; selector on raw DBF raises 'not applicable'"
    - "D-04: read() extended with company: str | None = None parameter; no filesystem-path arg ever added"
    - "D-05: ContaPlusData grows .subcta/.empresa/.grupos/.usuarios sibling attributes, each a typed result type"
    - "D-06: uniform strict — present-but-unreadable table aborts whole conversion; absent table is fine (None attribute, no error)"
    - "D-09: JournalRow gains subcuenta_nombre: str | None = None as final additive defaulted field"
    - "D-10: enrichment is subcuenta-level only; .cuenta stays code-only (no SUBCTA source for PGC-level names)"
    - "D-11: enrichment is best-effort — missing lookup key or absent SUBCTA.DBF yields None silently; present-but-unreadable SUBCTA aborts (D-06)"
    - "D-16: typed readers for SUBCTA/grupos/usuarios/empresa with defensive field-name resolution via _pick_column candidate lists"
    - "D-17: typed readers derived from schemas discovered across all 5 pii-test-data/ archives before committing"
    - "D-18: usuarios.dbf extracted in full including credential fields — fully local conversion, no data leaves user machine"
---

## Phase Goal

**As a** ContaPlus migrant, **I want to** convert a full ContaPlus backup `.zip` — including subaccount name enrichment — to a multi-sheet `.xlsx`, **so that** I have all my accounting data in one usable workbook without any install or upload.

<objective>
Implement the ZIP reading pipeline: sniffer upgrade, ZIP extraction module, SUBCTA lookup module, model extensions, journal enrichment, and `read()` dispatch. After this plan, `read(zip_bytes, company="Emp01")` returns a `ContaPlusData` with a journal enriched with `subcuenta_nombre` and with `subcta`/`grupos`/`usuarios`/`empresa` attributes populated when the corresponding DBFs are present.

Purpose: Deliver the core bytes-in → ContaPlusData-out ZIP path that all downstream consumers (CLI, XLSX renderer, future PWA) build on.

Output: _zip.py, _subcta.py (new), and four modified source files. All 13 reader test cases from Plan 02-01 are GREEN.
</objective>

<execution_context>
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/workflows/execute-plan.md
@C:/dev/contaplus-reader/.claude/gsd/plugins/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-zip-subaccounts/02-CONTEXT.md
@.planning/phases/02-zip-subaccounts/02-RESEARCH.md
@.planning/phases/02-zip-subaccounts/02-PATTERNS.md
@.planning/phases/02-zip-subaccounts/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/contaplus_reader/models.py (current):
  ContaPlusReadError(message, row_index=-1, column=None, original=None)
  JournalRow(fecha, cuenta, subcuenta, debe, haber, concepto)  -- frozen dataclass
  ContaPlusJournal(rows, skipped_memo=0, source_name=None)     -- frozen dataclass
  ContaPlusData(journal=None)                                   -- mutable dataclass

From src/contaplus_reader/_reader.py (current):
  _pick_column(field_set: set[str], candidates: tuple[str,...], *, kind: str) -> str
  _read_dbf_path(path: Path, *, source_name: str | None = None) -> ContaPlusJournal
  _DEBE_COL_CANDIDATES: tuple[str, ...]
  _HABER_COL_CANDIDATES: tuple[str, ...]

From src/contaplus_reader/_sniffer.py (current):
  sniff(data: bytes | io.IOBase) -> None  -- raises on unsupported, returns None on valid DBF
  _KNOWN_DBF_VERSION_BYTES: frozenset[int]

From src/contaplus_reader/__init__.py (current):
  read(data: bytes | BinaryIO, source_name: str | None = None) -> ContaPlusData
  -- calls sniff(data), then bytes_to_tmppath, then _read_dbf_path

From src/contaplus_reader/_bridge.py (current):
  bytes_to_tmppath(data: bytes | BinaryIO) -> contextmanager[Path]
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend models.py and upgrade sniffer; create _zip.py and _subcta.py</name>
  <files>
    src/contaplus_reader/models.py,
    src/contaplus_reader/_sniffer.py,
    src/contaplus_reader/_zip.py,
    src/contaplus_reader/_subcta.py
  </files>
  <read_first>
    - src/contaplus_reader/models.py (full file — understand JournalRow, ContaPlusData structure)
    - src/contaplus_reader/_sniffer.py (full file — understand current sniff() return type and branch to flip)
    - .planning/phases/02-zip-subaccounts/02-PATTERNS.md (§models.py, §_sniffer.py, §_zip.py, §_subcta.py patterns)
    - .planning/phases/02-zip-subaccounts/02-RESEARCH.md (§Pattern 1: ZIP Extraction, §Pattern 2: SUBCTA Lookup Dict, §Common Pitfalls 1-6)
  </read_first>
  <behavior>
    models.py:
    - JournalRow gains field subcuenta_nombre: str | None = None as the last field (after concepto) per D-09. Default None makes all existing JournalRow() calls backward compatible.
    - New frozen dataclass SubctaRow with single field: fields: dict[str, object]
    - New frozen dataclass SubctaTable(rows: tuple[SubctaRow, ...], source_name: str | None = None)
    - New frozen dataclass GenericTable(headers: tuple[str, ...], rows: tuple[tuple[object, ...], ...], source_name: str | None = None)
    - ContaPlusData gains four new defaulted attributes: subcta: SubctaTable | None = None, empresa: GenericTable | None = None, grupos: GenericTable | None = None, usuarios: GenericTable | None = None

    _sniffer.py:
    - sniff() return type changes from None to Literal["dbf", "zip"]
    - When first_byte == 0x50 (ZIP PK magic): return "zip" (no longer raises)
    - When first_byte in _KNOWN_DBF_VERSION_BYTES: return "dbf" (new explicit return)
    - When first_byte is None or not in either set: raise ContaPlusReadError as before (non-DBF non-ZIP bytes still rejected)

    _zip.py (new file):
    - Module docstring citing D-01/D-02/D-03/D-04 decision refs and RESEARCH.md Pitfalls 1-4
    - Imports: from __future__ import annotations, io, tempfile, zipfile, from pathlib import Path, from contaplus_reader.models import ContaPlusReadError
    - _safe_extract_zip(raw: bytes, extract_dir: Path) -> None: per-entry zip-slip check using target.relative_to(base); normalize member.filename with .replace("\\", "/") before constructing target path (Pitfall 2); raise ContaPlusReadError(row_index=-1, column=None, message=f"Unsafe ZIP entry: {member.filename!r}") on ValueError from relative_to; then call zf.extract(member, extract_dir)
    - _resolve_company_diario(extract_dir: Path, company: str | None) -> Path: rglob for files where p.name.lower() == "diario.dbf"; single-company with matching selector returns the path; single-company with wrong selector raises listing available dirs; multi-company with None raises listing available dirs; multi-company with matching selector returns matching path; no DIARIO found raises ContaPlusReadError
    - _find_sibling_dbf(diario_path: Path, basename_lower: str) -> Path | None: iterate diario_path.parent; return first candidate where candidate.is_file() and candidate.name.lower() == basename_lower; return None if not found

    _subcta.py (new file):
    - Imports: from __future__ import annotations, struct, from pathlib import Path, from dbfread import DBF, from contaplus_reader._reader import _pick_column, from contaplus_reader.models import ContaPlusReadError, SubctaRow, SubctaTable, GenericTable
    - _SUBCTA_COD_CANDIDATES: tuple[str, ...] = ("cod", "codigo")
    - _SUBCTA_TITULO_CANDIDATES: tuple[str, ...] = ("titulo", "descrip")
    - build_subcta_lookup(subcta_path: Path) -> dict[str, str | None]: open DBF with lowernames=True, encoding="cp850", ignore_missing_memofile=True; use _pick_column for cod and titulo fields; build dict {rec[cod_col].rstrip(): (rec[titulo_col] or "").rstrip() or None for rec in table if rec.get(cod_col)}; wrap non-ContaPlusReadError exceptions in ContaPlusReadError(row_index=-1, column=None, message=f"SUBCTA.DBF read error: {exc}", original=exc)
    - read_subcta_table(subcta_path: Path) -> SubctaTable: open DBF with lowernames=False (preserve original field names for full dump per D-13), encoding="cp850", ignore_missing_memofile=True; return SubctaTable(rows=tuple(SubctaRow(fields=dict(rec)) for rec in table), source_name=str(subcta_path))
    - read_table_raw(path: Path, table_name: str) -> GenericTable: open DBF with lowernames=False, encoding="cp850", ignore_missing_memofile=True; headers = tuple(f.name for f in table.fields); rows = tuple(tuple(rec[h] for h in headers) for rec in table); return GenericTable(headers=headers, rows=rows, source_name=str(path)); wrap exceptions as ContaPlusReadError
  </behavior>
  <action>
    models.py: Add subcuenta_nombre: str | None = None as the final field of JournalRow (after concepto). Add SubctaRow, SubctaTable, GenericTable dataclasses after ContaPlusJournal. Extend ContaPlusData with subcta, empresa, grupos, usuarios attributes, all defaulting to None. Add SubctaRow, SubctaTable, GenericTable to __all__ if an __all__ is present.

    _sniffer.py: Change function signature from `-> None` to `-> str`. Replace the ZIP-rejection branch (the `if first_byte == 0x50` hint) with `return "zip"`. Add `return "dbf"` at the end of the success path (currently no return statement — the function just falls through without raising). Update the docstring to document the return values "dbf" and "zip".

    _zip.py: Create new file. Copy zip-slip guard pattern verbatim from PATTERNS.md §_zip.py. Copy company resolution pattern verbatim. Copy sibling-file discovery pattern verbatim. Do NOT add TemporaryDirectory management here — that belongs in __init__.py (per RESEARCH.md Open Question 2 recommendation).

    _subcta.py: Create new file. Import _pick_column from contaplus_reader._reader. Define constants and three functions as specified in the behavior block: build_subcta_lookup, read_subcta_table, and read_table_raw. All three must be exported (no leading underscore).
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_reader.py -k "not zip" -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - models.py: JournalRow has subcuenta_nombre field; ContaPlusData has subcta/empresa/grupos/usuarios; SubctaTable, GenericTable, SubctaRow are importable
    - _sniffer.py: sniff(bytes_starting_with_0x50) returns "zip"; sniff(valid_dbf_bytes) returns "dbf"
    - _zip.py: importable; _safe_extract_zip, _resolve_company_diario, _find_sibling_dbf all present
    - _subcta.py: importable; build_subcta_lookup, read_subcta_table, read_table_raw all present
    - All 55 Phase 1 tests still pass (no regressions from model changes)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ZIP dispatch in _reader.py and __init__.py; make all reader tests GREEN</name>
  <files>
    src/contaplus_reader/_reader.py,
    src/contaplus_reader/__init__.py
  </files>
  <read_first>
    - src/contaplus_reader/_reader.py (full file — understand _read_dbf_path, JournalRow construction at lines 195-204)
    - src/contaplus_reader/__init__.py (full file — understand read() structure to extend)
    - .planning/phases/02-zip-subaccounts/02-PATTERNS.md (§_reader.py pattern: subcta_lookup param + JournalRow construction; §__init__.py pattern: extended read() with tmpdir lifecycle)
    - .planning/phases/02-zip-subaccounts/02-RESEARCH.md (§Common Pitfalls 1-6 — especially Pitfall 1: TemporaryDirectory premature cleanup, Pitfall 6: BytesIO exhausted)
  </read_first>
  <behavior>
    _reader.py:
    - _read_dbf_path gains subcta_lookup: dict[str, str | None] | None = None keyword parameter after source_name
    - In the JournalRow construction block: if subcta_lookup is not None, look up subcuenta_nombre = subcta_lookup.get(subcuenta) (per D-11: missing key returns None); if subcta_lookup is None, subcuenta_nombre = None; pass subcuenta_nombre=subcuenta_nombre to JournalRow(...)
    - All existing JournalRow calls are keyword-argument only — the new defaulted parameter does not break them

    __init__.py:
    - Add imports: import tempfile, from pathlib import Path, from contaplus_reader._zip import _safe_extract_zip, _resolve_company_diario, _find_sibling_dbf, from contaplus_reader._subcta import build_subcta_lookup, read_subcta_table, read_table_raw
    - read() signature: add company: str | None = None as third keyword parameter (D-04). Update docstring.
    - Normalize input to raw bytes once at the top: raw: bytes = data if isinstance(data, bytes) else data.read() (Pitfall 6 defense)
    - Call fmt = sniff(raw) — sniff now returns "dbf" or "zip"
    - If fmt == "zip": use `with tempfile.TemporaryDirectory(prefix="contaplus_") as tmpdir_str:` block; inside: extract_dir = Path(tmpdir_str); _safe_extract_zip(raw, extract_dir); diario_path = _resolve_company_diario(extract_dir, company); subcta_path = _find_sibling_dbf(diario_path, "subcta.dbf"); lookup = build_subcta_lookup(subcta_path) if subcta_path else {}; subcta_table = read_subcta_table(subcta_path) if subcta_path else None; journal = _read_dbf_path(diario_path, source_name=source_name, subcta_lookup=lookup); build ContaPlusData with journal + subcta + optional group tables; grupos_path = _find_sibling_dbf(diario_path, "grupos.dbf"); usuarios_path = _find_sibling_dbf(diario_path, "usuarios.dbf"); empresa_path = _find_sibling_dbf(diario_path, "empresa.dbf"); grupos = read_table_raw(grupos_path, "grupos.dbf") if grupos_path else None; usuarios = read_table_raw(usuarios_path, "usuarios.dbf") if usuarios_path else None; empresa = read_table_raw(empresa_path, "empresa.dbf") if empresa_path else None; return ContaPlusData(journal=journal, subcta=subcta_table, empresa=empresa, grupos=grupos, usuarios=usuarios) — all within the `with` block so tmpdir stays alive for all reads
    - If fmt == "dbf": if company is not None, raise ContaPlusReadError(row_index=-1, column=None, message="company selector is not applicable to a raw DBF input") (D-03); otherwise use existing bytes_to_tmppath path (unchanged from Phase 1, but call sniff(raw) not sniff(data) since data is now normalised)
    - The existing bytes_to_tmppath path: sniff already returned "dbf" so no second sniff needed; call `with bytes_to_tmppath(raw) as path:` (pass raw bytes, not original data)
    - Update __all__ if needed to export any new public symbols (none required — read() is the only public API)
  </behavior>
  <action>
    _reader.py: Add subcta_lookup parameter to _read_dbf_path. In the JournalRow construction block (around line 195-204), add the subcuenta_nombre lookup and pass it to JournalRow. Keep the JournalRow fields in order: fecha, cuenta, subcuenta, debe, haber, concepto, subcuenta_nombre.

    __init__.py: Replace the read() body entirely using the PATTERNS.md §__init__.py pattern. Add the import line: `from contaplus_reader._subcta import build_subcta_lookup, read_subcta_table, read_table_raw` (all three names are required — read_subcta_table populates data.subcta; read_table_raw populates data.grupos/usuarios/empresa). The TemporaryDirectory `with` block must encompass ALL dbfread calls (diario + subcta + group tables) so the tmpdir is not garbage collected mid-read (Pitfall 1). The bytes normalisation `raw = data if isinstance(data, bytes) else data.read()` must happen before the sniff call.
  </action>
  <verify>
    <automated>cd C:/dev/contaplus-reader && uv run pytest tests/test_reader.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 13 reader tests from Plan 02-01 are GREEN (or xpass).
    All 55 Phase 1 tests still pass.
    `uv run pytest tests/test_reader.py -q` shows 0 failures.
    read(zip_bytes) returns ContaPlusData; read(zip_bytes, company="Emp01") selects company; zip-slip raises; multi-company-no-selector raises; subcuenta_nombre populated for known codes; None for unknown codes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user ZIP bytes → TemporaryDirectory | Untrusted archive contents extracted; zip-slip defense runs before extraction |
| ZIP entry names → filesystem paths | Path traversal via "../" or absolute paths in entry names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / EoP | _zip.py _safe_extract_zip | mitigate | Per-entry validation: normalize backslashes then `target.relative_to(base)` before `zf.extract()`; ValueError raises ContaPlusReadError — extraction aborted before any file is written (verified against 4 attack vectors in RESEARCH.md §Security Domain) |
| T-02-02 | Tampering | _reader.py / _subcta.py DBF parsing | mitigate | `except (struct.error, ValueError, OSError, UnicodeDecodeError)` boundary wraps all dbfread calls in ContaPlusReadError; malformed DBF aborts conversion cleanly |
| T-02-03 | DoS | ZIP bomb (huge uncompressed archive) | accept | v1 files are user's own ContaPlus backups on local machine; no network-facing path; zip bomb out of scope (RESEARCH.md §Security Domain) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 |
</threat_model>

<verification>
`uv run pytest -q` — all 55 Phase 1 tests + all 13 new reader tests GREEN.
`python -c "from contaplus_reader import read; from contaplus_reader.models import SubctaTable, GenericTable"` — imports succeed.
`python -c "from contaplus_reader._sniffer import sniff; print(sniff(b'PK\x03\x04'))"` — prints "zip".
</verification>

<success_criteria>
- read(zip_bytes_single_company) -> ContaPlusData(journal=ContaPlusJournal with rows, subcta=SubctaTable or None)
- read(zip_bytes, company="Emp01") selects the correct company in a multi-company ZIP
- read(zip_bytes_no_company_selector) raises ContaPlusReadError listing ["Emp01", "Emp02"]
- read(zip_slip_bytes) raises ContaPlusReadError("Unsafe ZIP entry")
- read(dbf_bytes, company="Emp01") raises ContaPlusReadError("not applicable")
- JournalRow.subcuenta_nombre populated from SUBCTA; None when key absent or SUBCTA absent
- All Phase 1 tests still GREEN
</success_criteria>

<output>
Create `.planning/phases/02-zip-subaccounts/02-02-SUMMARY.md` when done.
</output>
