---
phase: 04-full-cli-pypi-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_cli.py
  - src/contaplus_reader/cli.py
autonomous: true
requirements:
  - CLI-03

must_haves:
  truths:
    - "Running contaplus2xlsx on a DBF prints a per-table report block with a 'Diario:' line and the correct row count"
    - "When skipped_memo > 0, the report includes a 'Memo lines skipped:' line with the count"
    - "In lenient mode, every problem entry is listed inline on stdout with table name, row reference, and reason"
    - "In strict mode (default), no 'Problems' section appears in stdout"
    - "The D-16 one-line summary is replaced; no 'journal rows, N sheet(s)' text appears"
  artifacts:
    - path: "tests/conftest.py"
      provides: "diario_with_memo_dbf session fixture — DIARIO.DBF with 2 valid rows + 1 both-zero memo row"
    - path: "tests/test_cli.py"
      provides: "CLI-03 test section: test_report_shows_diario_row_count, test_report_shows_skipped_memo_count, test_report_shows_problem_entries_in_lenient, test_report_no_problems_section_in_strict"
    - path: "src/contaplus_reader/cli.py"
      provides: "_print_report() helper + main() wired to call it; WR-07 block removed"
  key_links:
    - from: "src/contaplus_reader/cli.py _print_report()"
      to: "ContaPlusData attributes"
      via: "len(data.journal.rows), data.journal.skipped_memo, data.problems.entries"
      pattern: "len\\(data\\.journal\\.rows\\)"
    - from: "tests/test_cli.py"
      to: "tests/conftest.py"
      via: "diario_with_memo_dbf fixture parameter"
      pattern: "diario_with_memo_dbf"
---

<objective>
Implement the CLI-03 stdout conversion report (D-01/D-02/D-03): replace the D-16 one-line summary with a structured multi-line report showing per-table row counts, skipped-memo count, and (in lenient mode) all problem entries inline. Tests are written first (TDD), then the implementation.

Purpose: CLI-03 is a user-visible v1 requirement — every `contaplus2xlsx` conversion must produce a readable report of what was extracted. The existing one-liner (D-16) does not meet the requirement.

Output: Updated cli.py with `_print_report()`, updated test_cli.py with CLI-03 section, updated conftest.py with memo fixture.
</objective>

<execution_context>
@C:/Users/marc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/marc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-full-cli-pypi-publication/04-CONTEXT.md
@.planning/phases/04-full-cli-pypi-publication/04-RESEARCH.md
@.planning/phases/04-full-cli-pypi-publication/04-PATTERNS.md

<interfaces>
From src/contaplus_reader/cli.py (full file — read before modifying):
- Module-level: app = typer.Typer(add_completion=False); Console(stderr=True)
- main() deferred import at line 54: from contaplus_reader import ContaPlusReadError, read
- D-16 block to REMOVE: lines 123–139 (WR-07 sheet-count workaround + one-liner echo)
  - Includes: import io as _io; from openpyxl import load_workbook as _load_wb; the f-string echo
- The block immediately after render() + write_bytes() calls is the insertion point for _print_report(data, output_file)

From src/contaplus_reader/models.py — ContaPlusData attributes:
  data.journal: ContaPlusJournal | None
  data.journal.rows: tuple[JournalRow, ...]        # len() = Diario row count
  data.journal.skipped_memo: int                   # both-zero memo lines skipped
  data.subcta: SubctaTable | None; .rows: tuple[SubctaRow, ...]
  data.balan: GenericTable | None; .rows: tuple[tuple[object,...], ...]
  data.balance_cuenta: BalanceTable | None; .rows: tuple[BalanceRow, ...]
  data.balance_subcuenta: BalanceTable | None; .rows: tuple[BalanceRow, ...]
  data.venci, data.prede, data.amoinv, data.nivel: GenericTable | None; each has .rows
  data.empresa, data.grupos, data.usuarios: GenericTable | None; each has .rows
  data.problems: ProblemsReport | None
  data.problems.entries: tuple[ProblemEntry, ...]

From src/contaplus_reader/models.py — ProblemEntry fields:
  entry.table: str          # e.g. "DIARIO", "venci.dbf"
  entry.row_index: int      # 0-based; -1 = file/table-level
  entry.column: str         # field name, or "" when not row-specific
  entry.reason: str         # human-readable
  entry.value: str          # raw offending value, or ""

From tests/conftest.py — existing fixtures available for reuse:
  cp850_basic_dbf(tmp_path_factory) -> Path   # 3 valid rows, 0 memo
  diario_with_bad_row(tmp_path_factory) -> Path  # 3 rows: row 0 OK, row 1 bad subcta="BADCODE", row 2 OK
  _build_diario_dbf(target, rows, *, codepage) -> Path  # internal builder (not a fixture)
  diario_dbf_builder(tmp_path_factory) -> Callable  # factory for custom rows

From tests/conftest.py — _build_diario_dbf() row dict schema (required fields):
  fecha, subcta, concepto, debe, haber, eurodebe, eurohaber, contra
  (Field order and types must match the existing _DIARIO_SPEC string in conftest.py)

Test runner pattern from tests/test_cli.py:
  runner = CliRunner()
  result = runner.invoke(app, [str(input_path), str(out_path)])
  # stdout: result.output
  # stderr: result.stderr (if hasattr)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diario_with_memo_dbf fixture + CLI-03 tests (RED)</name>
  <files>tests/conftest.py, tests/test_cli.py</files>
  <read_first>
    - tests/conftest.py (full file) — understand _build_diario_dbf signature, _DIARIO_SPEC field names, existing fixture patterns (cp850_basic_dbf at line 121, diario_with_bad_row at line 551)
    - tests/test_cli.py (full file) — find the last test (line 259) and the existing CliRunner + assertion patterns (lines 1–25, 55–84)
    - src/contaplus_reader/models.py lines 168–195 — ProblemEntry fields (table, row_index, column, reason, value)
  </read_first>
  <behavior>
    New fixture in conftest.py:
    - diario_with_memo_dbf: session-scoped, produces DIARIO.DBF with 2 valid journal rows + 1 both-zero memo row (debe=0.0, haber=0.0). The reader skips the both-zero row and sets skipped_memo=1.

    New test section in test_cli.py after the last existing test (line ~259), titled "# Report output (CLI-03 D-01/D-02/D-03)":

    - test_report_shows_diario_row_count: cp850_basic_dbf (3 rows) → assert "Diario:" in result.output AND "3" in result.output AND result.exit_code == 0
    - test_report_shows_skipped_memo_count: diario_with_memo_dbf (2 valid + 1 memo) → assert "Memo lines skipped:" in result.output AND result.exit_code == 0
    - test_report_shows_problem_entries_in_lenient: diario_with_bad_row + --lenient → assert "Problems" in result.output AND "DIARIO" in result.output AND result.exit_code == 0
    - test_report_no_problems_section_in_strict: cp850_basic_dbf (no bad rows, strict default) → assert "Problems" not in result.output AND result.exit_code == 0

    All four tests will fail RED until Task 2 replaces the D-16 one-liner with _print_report().
  </behavior>
  <action>
    In tests/conftest.py: add a new session-scoped fixture `diario_with_memo_dbf` using the `diario_dbf_builder` factory (already exported at line 219). The fixture builds a DIARIO.DBF with 3 rows total: two normal rows (eurodebe=100.0/eurohaber=0.0 and eurodebe=0.0/eurohaber=100.0) plus one both-zero row (eurodebe=0.0, eurohaber=0.0, debe=0.0, haber=0.0). If _build_diario_dbf does not support debe/haber directly (it uses eurodebe/eurohaber) verify the field names from _DIARIO_SPEC before writing. Place the fixture after the existing `diario_with_bad_row` fixture (around line 590) to keep thematic grouping.

    In tests/test_cli.py: append a new test section after the final existing test. Use the CliRunner pattern from the existing file. Assert result.output (stdout) for the four behaviors listed in the behavior block. The tests check for "Diario:", "Memo lines skipped:", "Problems", and "DIARIO" in stdout — none of these strings appear in the current D-16 one-liner, so all four tests MUST be RED after this task.

    Run `uv run pytest tests/test_cli.py -x -k "test_report"` to confirm RED (expected: 4 failures due to missing _print_report). If tests pass, the existing output already contains the strings — review the existing D-16 echo and adjust assertion strings.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -x -k "test_report or test_memo" 2>&1 | head -30</automated>
  </verify>
  <done>4 new tests exist in test_cli.py; diario_with_memo_dbf fixture exists in conftest.py; all 4 new tests fail RED (AssertionError on string not found in output). Existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _print_report() in cli.py (GREEN)</name>
  <files>src/contaplus_reader/cli.py</files>
  <read_first>
    - src/contaplus_reader/cli.py (full file) — identify lines 123–139 (the D-16 WR-07 block to remove); identify the deferred import at line 54; understand the stdout/stderr split (Console(stderr=True) pattern)
    - .planning/phases/04-full-cli-pypi-publication/04-PATTERNS.md § "src/contaplus_reader/cli.py" — the exact _print_report() body, the WR-07 block diff, and the deferred import change
    - .planning/phases/04-full-cli-pypi-publication/04-RESEARCH.md § "Pattern 1: Multi-line stdout report" — the per-table ordering (matching xlsx.py render() order)
  </read_first>
  <behavior>
    After _print_report() is implemented and called:
    - _print_report returns None; it calls typer.echo() exclusively (stdout); no Rich console calls
    - Report starts with: "Converted: {output_file}" then a blank line
    - Per-table section prints one line per non-None table in this order:
      Diario (len(data.journal.rows) rows), Memo lines skipped (only when >0),
      Subcuentas, Balance (raw), Sumas y Saldos (ctas), Sumas y Saldos (subs),
      then Vencimientos/Predefinidos/Amortizaciones/Niveles/Empresa/Grupos/Usuarios (only when non-None)
    - Problems section: only when data.problems and data.problems.entries is non-empty;
      prints "  Problems (N):" then one line per entry: "[{entry.table}] row {entry.row_index}, col {entry.column}: {entry.reason} (value: {entry.value!r})"
      (row_index=-1 prints "file level" instead of "row -1")
    - The WR-07 block (lines 123–139) is gone; no `import io as _io` or `from openpyxl import load_workbook as _load_wb` inside main()
    - ContaPlusData is imported in the deferred import: from contaplus_reader import ContaPlusData, ContaPlusReadError, read
  </behavior>
  <action>
    Edit src/contaplus_reader/cli.py:

    1. Remove lines 123–139 entirely (the WR-07 block containing the sheet-count openpyxl workaround and the D-16 one-liner echo). The inline `import io as _io` and `from openpyxl import load_workbook as _load_wb` inside main() go with it.

    2. Change the deferred import at line 54 from:
       `from contaplus_reader import ContaPlusReadError, read`
       to:
       `from contaplus_reader import ContaPlusData, ContaPlusReadError, read`

    3. Insert a call `_print_report(data, output_file)` at the position where lines 123–139 were removed (after `output_file.write_bytes(xlsx_bytes)` succeeds).

    4. Add a module-level function `_print_report(data: ContaPlusData, output_file: Path) -> None` above the `@app.command()` decorator. Function body is in 04-PATTERNS.md § "src/contaplus_reader/cli.py" — implement it exactly as described there. Key rules per D-01/D-02/D-03 and the behavior block above:
       - All output via `typer.echo()` (stdout, not the Rich console)
       - Per-table ordering matches xlsx.py render() order (also in 04-RESEARCH.md Pattern 1)
       - `getattr(data, attr, None)` for the dynamic attrs (venci/prede/amoinv/nivel/empresa/grupos/usuarios)
       - Problems section only when `data.problems and data.problems.entries`
       - row_ref: "row {entry.row_index}" if entry.row_index >= 0 else "file level"
       - No column segment when entry.column is "" or falsy; no value segment when entry.value is "" or falsy

    Do NOT add module-level imports for `io` or `openpyxl` — those were only used by the WR-07 block being removed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q 2>&1</automated>
  </verify>
  <done>All tests in test_cli.py pass (including the 4 new CLI-03 tests). No "Traceback" in any test failure. The D-16 strings ("journal rows", "sheet(s)") no longer appear in success output. "Diario:" and "Converted:" appear in success output. `uv run pytest tests/test_cli.py -q` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContaPlusData → _print_report() | Model data rendered to stdout; no external input beyond what was already validated by read() |
| test output → assertions | CliRunner captures stdout/stderr; no external process boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | _print_report() stdout | accept | Data already validated by read(); report is user-initiated on their own files; no PII exposure beyond what the file contains |
| T-04-02 | Tampering | tests/conftest.py memo fixture | accept | Synthetic fixture; no real data; session-scoped so immutable during test run |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Plan 01 adds no new packages — dev tool additions are in Plan 02 (python-semantic-release, lockfile-committed) |
</threat_model>

<verification>
After both tasks complete:
- `uv run pytest -q` exits 0 (full suite, all phases)
- `uv run pytest tests/test_cli.py -q` exits 0 (all CLI tests including 4 new CLI-03 tests)
- No "Traceback" in test output
- `python -c "from contaplus_reader.cli import app"` exits 0 (import clean)
- Spot-check: run `uv run contaplus2xlsx --help` prints successfully
</verification>

<success_criteria>
- CLI-03 requirement satisfied: contaplus2xlsx prints per-table row counts, skipped-memo count (when >0), and problem entries (in lenient mode) to stdout after every conversion
- D-01 satisfied: multi-line report block replaces D-16 one-liner on every conversion
- D-02 satisfied: per-table counts derived from ContaPlusData attributes (not workbook re-parse); WR-07 block removed
- D-03 satisfied: lenient mode lists full problem entry detail on stdout; strict mode has no Problems section
- 4 new CLI-03 tests pass GREEN; 0 regressions in existing tests
</success_criteria>

<output>
Create `.planning/phases/04-full-cli-pypi-publication/04-01-SUMMARY.md` when done
</output>
